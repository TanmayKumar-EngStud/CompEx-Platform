# Folder Structure: 

* **app**: (main file - most complex one!) Frontend
* **prisma**: consists of table definition and migration scripts 
* **public**: it consists of images and general SVG images
* **utils**: it has files to connect the .env variables with the server
* **service**: Backend (functions to fetch question data from the database using prisma as wrapper)
