{
  "type": "OBJECT",
  "properties": {
    "options": {
      "type": "ARRAY",
      "items": { "type": "STRING" }
    },
    "answer": {
      "type": "ARRAY",
      "items": {
        "type": "OBJECT",
        "properties": {
          "statement": { "type": "STRING" },
          "response": { "type": "STRING" },
          "explanation": { "type": "STRING" }
        },
        "required": ["statement", "response", "explanation"]
      }
    }
  },
  "required": ["options", "answer"]
}
