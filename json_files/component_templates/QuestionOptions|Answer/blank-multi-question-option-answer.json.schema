{
  "type": "OBJECT",
  "properties": {
    "options": {
      "type": "ARRAY",
      "items": {
        "type": "OBJECT",
        "additionalProperties": {
          "type": "OBJECT",
          "properties": {
            "text": { "type": "STRING" },
            "explanation": { "type": "STRING" }
          },
          "required": ["text", "explanation"]
        }
      }
    },
    "answer": {
      "type": "ARRAY",
      "items": { "type": "STRING" }
    }
  },
  "required": ["options", "answer"]
}
