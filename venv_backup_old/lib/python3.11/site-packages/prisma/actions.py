# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ProblemsSetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProblemsSet]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProblemsSet.prisma().query_raw(
            'SELECT * FROM ProblemsSet WHERE problemsSetId = $1',
            1103527590,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProblemsSet
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProblemsSet.prisma().query_first(
            'SELECT * FROM ProblemsSet WHERE sectionid = $1',
            377401575,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProblemsSetCreateInput,
        include: Optional[types.ProblemsSetInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProblemsSet record.

        Parameters
        ----------
        data
            ProblemsSet record data
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The created ProblemsSet record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProblemsSet record from just the required fields
        problemsset = ProblemsSet.prisma().create(
            data={
                # data to create a ProblemsSet record
                'sectionid': 662824084,
                'examtypeid': 1147902781,
                'title': 'cadfabfehe',
                'type': 'dgiiaaijj',
                'content': Json({'bfaiacjjfc': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProblemsSetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProblemsSet records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProblemsSet record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProblemsSet.prisma().create_many(
            data=[
                {
                    # data to create a ProblemsSet record
                    'sectionid': 486256185,
                    'examtypeid': 1062517886,
                    'title': 'cghideieh',
                    'type': 'biabhbdai',
                    'content': Json({'idghgaicb': True}),
                },
                {
                    # data to create a ProblemsSet record
                    'sectionid': 595337866,
                    'examtypeid': 790425851,
                    'title': 'cbbbjbfcii',
                    'type': 'bbejhfidcb',
                    'content': Json({'bgeecijdgg': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProblemsSetWhereUniqueInput,
        include: Optional[types.ProblemsSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProblemsSet record.

        Parameters
        ----------
        where
            ProblemsSet filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The deleted ProblemsSet record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemsset = ProblemsSet.prisma().delete(
            where={
                'problemsSetId': 1388290519,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProblemsSetWhereUniqueInput,
        include: Optional[types.ProblemsSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProblemsSet record.

        Parameters
        ----------
        where
            ProblemsSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The found ProblemsSet record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemsset = ProblemsSet.prisma().find_unique(
            where={
                'problemsSetId': 1647418052,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProblemsSetWhereUniqueInput,
        include: Optional[types.ProblemsSetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProblemsSet record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProblemsSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The found ProblemsSet record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemsset = ProblemsSet.prisma().find_unique_or_raise(
            where={
                'problemsSetId': 1675546029,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
        include: Optional[types.ProblemsSetInclude] = None,
        order: Optional[Union[types.ProblemsSetOrderByInput, List[types.ProblemsSetOrderByInput]]] = None,
        distinct: Optional[List[types.ProblemsSetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProblemsSet records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProblemsSet records returned
        skip
            Ignore the first N results
        where
            ProblemsSet filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProblemsSet model
        order
            Order the returned ProblemsSet records by any field
        distinct
            Filter ProblemsSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProblemsSet]
            The list of all ProblemsSet records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProblemsSet records
        problemssets = ProblemsSet.prisma().find_many(take=10)

        # find the first 5 ProblemsSet records ordered by the examtypeid field
        problemssets = ProblemsSet.prisma().find_many(
            take=5,
            order={
                'examtypeid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
        include: Optional[types.ProblemsSetInclude] = None,
        order: Optional[Union[types.ProblemsSetOrderByInput, List[types.ProblemsSetOrderByInput]]] = None,
        distinct: Optional[List[types.ProblemsSetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProblemsSet record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProblemsSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProblemsSet model
        order
            Order the returned ProblemsSet records by any field
        distinct
            Filter ProblemsSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProblemsSet
            The first ProblemsSet record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProblemsSet record ordered by the title field
        problemsset = ProblemsSet.prisma().find_first(
            skip=1,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
        include: Optional[types.ProblemsSetInclude] = None,
        order: Optional[Union[types.ProblemsSetOrderByInput, List[types.ProblemsSetOrderByInput]]] = None,
        distinct: Optional[List[types.ProblemsSetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProblemsSet record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProblemsSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProblemsSet model
        order
            Order the returned ProblemsSet records by any field
        distinct
            Filter ProblemsSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProblemsSet
            The first ProblemsSet record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProblemsSet record ordered by the type field
        problemsset = ProblemsSet.prisma().find_first_or_raise(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProblemsSetUpdateInput,
        where: types.ProblemsSetWhereUniqueInput,
        include: Optional[types.ProblemsSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProblemsSet record.

        Parameters
        ----------
        data
            ProblemsSet record data specifying what to update
        where
            ProblemsSet filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The updated ProblemsSet record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        problemsset = ProblemsSet.prisma().update(
            where={
                'problemsSetId': 1767274722,
            },
            data={
                # data to update the ProblemsSet record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProblemsSetWhereUniqueInput,
        data: types.ProblemsSetUpsertInput,
        include: Optional[types.ProblemsSetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProblemsSet filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProblemsSet model

        Returns
        -------
        prisma.models.ProblemsSet
            The created or updated ProblemsSet record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemsset = ProblemsSet.prisma().upsert(
            where={
                'problemsSetId': 326272115,
            },
            data={
                'create': {
                    'problemsSetId': 326272115,
                    'sectionid': 595337866,
                    'examtypeid': 790425851,
                    'title': 'cbbbjbfcii',
                    'type': 'bbejhfidcb',
                    'content': Json({'bgeecijdgg': True}),
                },
                'update': {
                    'sectionid': 595337866,
                    'examtypeid': 790425851,
                    'title': 'cbbbjbfcii',
                    'type': 'bbejhfidcb',
                    'content': Json({'bgeecijdgg': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProblemsSetUpdateManyMutationInput,
        where: types.ProblemsSetWhereInput,
    ) -> int:
        """Update multiple ProblemsSet records

        Parameters
        ----------
        data
            ProblemsSet data to update the selected ProblemsSet records to
        where
            Filter to select the ProblemsSet records to update

        Returns
        -------
        int
            The total number of ProblemsSet records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProblemsSet records
        total = ProblemsSet.prisma().update_many(
            data={
                'content': Json({'bdedcabahc': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProblemsSet records present in the database

        Parameters
        ----------
        select
            Select the ProblemsSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProblemsSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProblemsSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProblemsSet.prisma().count()

        # results: prisma.types.ProblemsSetCountAggregateOutput
        results = ProblemsSet.prisma().count(
            select={
                '_all': True,
                'mocksectionid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProblemsSetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
    ) -> types.ProblemsSetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProblemsSetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProblemsSetWhereInput] = None,
        cursor: Optional[types.ProblemsSetWhereUniqueInput] = None,
    ) -> Union[int, types.ProblemsSetCountAggregateOutput]:
        """Count the number of ProblemsSet records present in the database

        Parameters
        ----------
        select
            Select the ProblemsSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProblemsSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProblemsSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProblemsSet.prisma().count()

        # results: prisma.types.ProblemsSetCountAggregateOutput
        results = ProblemsSet.prisma().count(
            select={
                '_all': True,
                'mockquestionnumber': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProblemsSetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProblemsSetWhereInput] = None
    ) -> int:
        """Delete multiple ProblemsSet records.

        Parameters
        ----------
        where
            Optional ProblemsSet filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProblemsSet records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProblemsSet records
        total = ProblemsSet.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProblemsSetScalarFieldKeys'],
        *,
        where: Optional['types.ProblemsSetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProblemsSetAvgAggregateInput'] = None,
        sum: Optional['types.ProblemsSetSumAggregateInput'] = None,
        min: Optional['types.ProblemsSetMinAggregateInput'] = None,
        max: Optional['types.ProblemsSetMaxAggregateInput'] = None,
        having: Optional['types.ProblemsSetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProblemsSetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProblemsSetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProblemsSetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProblemsSetGroupByOutput']:
        """Group ProblemsSet records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProblemsSet fields to group records by
        where
            ProblemsSet filter to select records
        take
            Limit the maximum number of ProblemsSet records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProblemsSetGroupByOutput]
            A list of dictionaries representing the ProblemsSet record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProblemsSet records by problemsSetId values
        # and count how many records are in each group
        results = ProblemsSet.prisma().group_by(
            ['problemsSetId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class examtypesActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.examtypes]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = examtypes.prisma().query_raw(
            'SELECT * FROM examtypes WHERE examtypeid = $1',
            675780521,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.examtypes
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = examtypes.prisma().query_first(
            'SELECT * FROM examtypes WHERE name = $1',
            'heejgedji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.examtypesCreateInput,
        include: Optional[types.examtypesInclude] = None
    ) -> _PrismaModelT:
        """Create a new examtypes record.

        Parameters
        ----------
        data
            examtypes record data
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The created examtypes record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a examtypes record from just the required fields
        examtypes = examtypes.prisma().create(
            data={
                # data to create a examtypes record
                'name': 'bjgjgibgbf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.examtypesCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple examtypes records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of examtypes record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = examtypes.prisma().create_many(
            data=[
                {
                    # data to create a examtypes record
                    'name': 'bbbgbhfjge',
                },
                {
                    # data to create a examtypes record
                    'name': 'igbehcbab',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.examtypesWhereUniqueInput,
        include: Optional[types.examtypesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single examtypes record.

        Parameters
        ----------
        where
            examtypes filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The deleted examtypes record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        examtypes = examtypes.prisma().delete(
            where={
                'examtypeid': 1303003706,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.examtypesWhereUniqueInput,
        include: Optional[types.examtypesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique examtypes record.

        Parameters
        ----------
        where
            examtypes filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The found examtypes record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        examtypes = examtypes.prisma().find_unique(
            where={
                'examtypeid': 1686638315,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.examtypesWhereUniqueInput,
        include: Optional[types.examtypesInclude] = None
    ) -> _PrismaModelT:
        """Find a unique examtypes record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            examtypes filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The found examtypes record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        examtypes = examtypes.prisma().find_unique_or_raise(
            where={
                'examtypeid': 2000430152,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
        include: Optional[types.examtypesInclude] = None,
        order: Optional[Union[types.examtypesOrderByInput, List[types.examtypesOrderByInput]]] = None,
        distinct: Optional[List[types.examtypesScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple examtypes records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of examtypes records returned
        skip
            Ignore the first N results
        where
            examtypes filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned examtypes model
        order
            Order the returned examtypes records by any field
        distinct
            Filter examtypes records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.examtypes]
            The list of all examtypes records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 examtypes records
        examtypes = examtypes.prisma().find_many(take=10)

        # find the first 5 examtypes records ordered by the description field
        examtypes = examtypes.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
        include: Optional[types.examtypesInclude] = None,
        order: Optional[Union[types.examtypesOrderByInput, List[types.examtypesOrderByInput]]] = None,
        distinct: Optional[List[types.examtypesScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single examtypes record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            examtypes filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned examtypes model
        order
            Order the returned examtypes records by any field
        distinct
            Filter examtypes records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.examtypes
            The first examtypes record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second examtypes record ordered by the examtypeid field
        examtypes = examtypes.prisma().find_first(
            skip=1,
            order={
                'examtypeid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
        include: Optional[types.examtypesInclude] = None,
        order: Optional[Union[types.examtypesOrderByInput, List[types.examtypesOrderByInput]]] = None,
        distinct: Optional[List[types.examtypesScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single examtypes record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            examtypes filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned examtypes model
        order
            Order the returned examtypes records by any field
        distinct
            Filter examtypes records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.examtypes
            The first examtypes record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second examtypes record ordered by the name field
        examtypes = examtypes.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.examtypesUpdateInput,
        where: types.examtypesWhereUniqueInput,
        include: Optional[types.examtypesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single examtypes record.

        Parameters
        ----------
        data
            examtypes record data specifying what to update
        where
            examtypes filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The updated examtypes record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        examtypes = examtypes.prisma().update(
            where={
                'examtypeid': 1868141281,
            },
            data={
                # data to update the examtypes record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.examtypesWhereUniqueInput,
        data: types.examtypesUpsertInput,
        include: Optional[types.examtypesInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            examtypes filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned examtypes model

        Returns
        -------
        prisma.models.examtypes
            The created or updated examtypes record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        examtypes = examtypes.prisma().upsert(
            where={
                'examtypeid': 1860847622,
            },
            data={
                'create': {
                    'examtypeid': 1860847622,
                    'name': 'igbehcbab',
                },
                'update': {
                    'name': 'igbehcbab',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.examtypesUpdateManyMutationInput,
        where: types.examtypesWhereInput,
    ) -> int:
        """Update multiple examtypes records

        Parameters
        ----------
        data
            examtypes data to update the selected examtypes records to
        where
            Filter to select the examtypes records to update

        Returns
        -------
        int
            The total number of examtypes records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all examtypes records
        total = examtypes.prisma().update_many(
            data={
                'description': 'beeifcbebf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
    ) -> int:
        """Count the number of examtypes records present in the database

        Parameters
        ----------
        select
            Select the examtypes fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            examtypes filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.examtypesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = examtypes.prisma().count()

        # results: prisma.types.examtypesCountAggregateOutput
        results = examtypes.prisma().count(
            select={
                '_all': True,
                'examtypeid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.examtypesCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
    ) -> types.examtypesCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.examtypesCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.examtypesWhereInput] = None,
        cursor: Optional[types.examtypesWhereUniqueInput] = None,
    ) -> Union[int, types.examtypesCountAggregateOutput]:
        """Count the number of examtypes records present in the database

        Parameters
        ----------
        select
            Select the examtypes fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            examtypes filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.examtypesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = examtypes.prisma().count()

        # results: prisma.types.examtypesCountAggregateOutput
        results = examtypes.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.examtypesCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.examtypesWhereInput] = None
    ) -> int:
        """Delete multiple examtypes records.

        Parameters
        ----------
        where
            Optional examtypes filter to find the records to be deleted

        Returns
        -------
        int
            The total number of examtypes records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all examtypes records
        total = examtypes.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.examtypesScalarFieldKeys'],
        *,
        where: Optional['types.examtypesWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.examtypesAvgAggregateInput'] = None,
        sum: Optional['types.examtypesSumAggregateInput'] = None,
        min: Optional['types.examtypesMinAggregateInput'] = None,
        max: Optional['types.examtypesMaxAggregateInput'] = None,
        having: Optional['types.examtypesScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.examtypesCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.examtypesScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.examtypesScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.examtypesGroupByOutput']:
        """Group examtypes records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar examtypes fields to group records by
        where
            examtypes filter to select records
        take
            Limit the maximum number of examtypes records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.examtypesGroupByOutput]
            A list of dictionaries representing the examtypes record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group examtypes records by description values
        # and count how many records are in each group
        results = examtypes.prisma().group_by(
            ['description'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class mocktestrankingsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.mocktestrankings]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = mocktestrankings.prisma().query_raw(
            'SELECT * FROM mocktestrankings WHERE rankingid = $1',
            1628650740,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.mocktestrankings
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = mocktestrankings.prisma().query_first(
            'SELECT * FROM mocktestrankings WHERE userid = $1',
            1249606685,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.mocktestrankingsCreateInput,
        include: Optional[types.mocktestrankingsInclude] = None
    ) -> _PrismaModelT:
        """Create a new mocktestrankings record.

        Parameters
        ----------
        data
            mocktestrankings record data
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The created mocktestrankings record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a mocktestrankings record from just the required fields
        mocktestrankings = mocktestrankings.prisma().create(
            data={
                # data to create a mocktestrankings record
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.mocktestrankingsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple mocktestrankings records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of mocktestrankings record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = mocktestrankings.prisma().create_many(
            data=[
                {
                    # data to create a mocktestrankings record
                },
                {
                    # data to create a mocktestrankings record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.mocktestrankingsWhereUniqueInput,
        include: Optional[types.mocktestrankingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single mocktestrankings record.

        Parameters
        ----------
        where
            mocktestrankings filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The deleted mocktestrankings record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestrankings = mocktestrankings.prisma().delete(
            where={
                'rankingid': 835903122,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.mocktestrankingsWhereUniqueInput,
        include: Optional[types.mocktestrankingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique mocktestrankings record.

        Parameters
        ----------
        where
            mocktestrankings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The found mocktestrankings record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestrankings = mocktestrankings.prisma().find_unique(
            where={
                'rankingid': 763719779,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.mocktestrankingsWhereUniqueInput,
        include: Optional[types.mocktestrankingsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique mocktestrankings record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            mocktestrankings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The found mocktestrankings record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestrankings = mocktestrankings.prisma().find_unique_or_raise(
            where={
                'rankingid': 429995104,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
        include: Optional[types.mocktestrankingsInclude] = None,
        order: Optional[Union[types.mocktestrankingsOrderByInput, List[types.mocktestrankingsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestrankingsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple mocktestrankings records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of mocktestrankings records returned
        skip
            Ignore the first N results
        where
            mocktestrankings filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestrankings model
        order
            Order the returned mocktestrankings records by any field
        distinct
            Filter mocktestrankings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.mocktestrankings]
            The list of all mocktestrankings records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 mocktestrankings records
        mocktestrankings = mocktestrankings.prisma().find_many(take=10)

        # find the first 5 mocktestrankings records ordered by the mocktestid field
        mocktestrankings = mocktestrankings.prisma().find_many(
            take=5,
            order={
                'mocktestid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
        include: Optional[types.mocktestrankingsInclude] = None,
        order: Optional[Union[types.mocktestrankingsOrderByInput, List[types.mocktestrankingsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestrankingsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single mocktestrankings record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktestrankings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestrankings model
        order
            Order the returned mocktestrankings records by any field
        distinct
            Filter mocktestrankings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktestrankings
            The first mocktestrankings record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktestrankings record ordered by the rank field
        mocktestrankings = mocktestrankings.prisma().find_first(
            skip=1,
            order={
                'rank': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
        include: Optional[types.mocktestrankingsInclude] = None,
        order: Optional[Union[types.mocktestrankingsOrderByInput, List[types.mocktestrankingsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestrankingsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single mocktestrankings record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktestrankings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestrankings model
        order
            Order the returned mocktestrankings records by any field
        distinct
            Filter mocktestrankings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktestrankings
            The first mocktestrankings record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktestrankings record ordered by the percentile field
        mocktestrankings = mocktestrankings.prisma().find_first_or_raise(
            skip=1,
            order={
                'percentile': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.mocktestrankingsUpdateInput,
        where: types.mocktestrankingsWhereUniqueInput,
        include: Optional[types.mocktestrankingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single mocktestrankings record.

        Parameters
        ----------
        data
            mocktestrankings record data specifying what to update
        where
            mocktestrankings filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The updated mocktestrankings record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        mocktestrankings = mocktestrankings.prisma().update(
            where={
                'rankingid': 1775811865,
            },
            data={
                # data to update the mocktestrankings record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.mocktestrankingsWhereUniqueInput,
        data: types.mocktestrankingsUpsertInput,
        include: Optional[types.mocktestrankingsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            mocktestrankings filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned mocktestrankings model

        Returns
        -------
        prisma.models.mocktestrankings
            The created or updated mocktestrankings record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestrankings = mocktestrankings.prisma().upsert(
            where={
                'rankingid': 893145566,
            },
            data={
                'create': {
                    'rankingid': 893145566,
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.mocktestrankingsUpdateManyMutationInput,
        where: types.mocktestrankingsWhereInput,
    ) -> int:
        """Update multiple mocktestrankings records

        Parameters
        ----------
        data
            mocktestrankings data to update the selected mocktestrankings records to
        where
            Filter to select the mocktestrankings records to update

        Returns
        -------
        int
            The total number of mocktestrankings records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all mocktestrankings records
        total = mocktestrankings.prisma().update_many(
            data={
                'rankingid': 995405759
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of mocktestrankings records present in the database

        Parameters
        ----------
        select
            Select the mocktestrankings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktestrankings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestrankingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktestrankings.prisma().count()

        # results: prisma.types.mocktestrankingsCountAggregateOutput
        results = mocktestrankings.prisma().count(
            select={
                '_all': True,
                'userid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.mocktestrankingsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
    ) -> types.mocktestrankingsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.mocktestrankingsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestrankingsWhereInput] = None,
        cursor: Optional[types.mocktestrankingsWhereUniqueInput] = None,
    ) -> Union[int, types.mocktestrankingsCountAggregateOutput]:
        """Count the number of mocktestrankings records present in the database

        Parameters
        ----------
        select
            Select the mocktestrankings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktestrankings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestrankingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktestrankings.prisma().count()

        # results: prisma.types.mocktestrankingsCountAggregateOutput
        results = mocktestrankings.prisma().count(
            select={
                '_all': True,
                'mocktestid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.mocktestrankingsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.mocktestrankingsWhereInput] = None
    ) -> int:
        """Delete multiple mocktestrankings records.

        Parameters
        ----------
        where
            Optional mocktestrankings filter to find the records to be deleted

        Returns
        -------
        int
            The total number of mocktestrankings records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all mocktestrankings records
        total = mocktestrankings.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.mocktestrankingsScalarFieldKeys'],
        *,
        where: Optional['types.mocktestrankingsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.mocktestrankingsAvgAggregateInput'] = None,
        sum: Optional['types.mocktestrankingsSumAggregateInput'] = None,
        min: Optional['types.mocktestrankingsMinAggregateInput'] = None,
        max: Optional['types.mocktestrankingsMaxAggregateInput'] = None,
        having: Optional['types.mocktestrankingsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.mocktestrankingsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.mocktestrankingsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.mocktestrankingsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.mocktestrankingsGroupByOutput']:
        """Group mocktestrankings records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar mocktestrankings fields to group records by
        where
            mocktestrankings filter to select records
        take
            Limit the maximum number of mocktestrankings records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.mocktestrankingsGroupByOutput]
            A list of dictionaries representing the mocktestrankings record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group mocktestrankings records by rank values
        # and count how many records are in each group
        results = mocktestrankings.prisma().group_by(
            ['rank'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class mocktestsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.mocktests]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = mocktests.prisma().query_raw(
            'SELECT * FROM mocktests WHERE mocktestid = $1',
            2102736524,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.mocktests
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = mocktests.prisma().query_first(
            'SELECT * FROM mocktests WHERE difficulty = $1',
            271520213,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.mocktestsCreateInput,
        include: Optional[types.mocktestsInclude] = None
    ) -> _PrismaModelT:
        """Create a new mocktests record.

        Parameters
        ----------
        data
            mocktests record data
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The created mocktests record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a mocktests record from just the required fields
        mocktests = mocktests.prisma().create(
            data={
                # data to create a mocktests record
                'date': datetime.datetime.utcnow(),
                'starttime': datetime.datetime.utcnow(),
                'endtime': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.mocktestsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple mocktests records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of mocktests record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = mocktests.prisma().create_many(
            data=[
                {
                    # data to create a mocktests record
                    'date': datetime.datetime.utcnow(),
                    'starttime': datetime.datetime.utcnow(),
                    'endtime': datetime.datetime.utcnow(),
                },
                {
                    # data to create a mocktests record
                    'date': datetime.datetime.utcnow(),
                    'starttime': datetime.datetime.utcnow(),
                    'endtime': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.mocktestsWhereUniqueInput,
        include: Optional[types.mocktestsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single mocktests record.

        Parameters
        ----------
        where
            mocktests filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The deleted mocktests record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktests = mocktests.prisma().delete(
            where={
                'mocktestid': 456633834,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.mocktestsWhereUniqueInput,
        include: Optional[types.mocktestsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique mocktests record.

        Parameters
        ----------
        where
            mocktests filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The found mocktests record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktests = mocktests.prisma().find_unique(
            where={
                'mocktestid': 2058258651,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.mocktestsWhereUniqueInput,
        include: Optional[types.mocktestsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique mocktests record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            mocktests filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The found mocktests record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktests = mocktests.prisma().find_unique_or_raise(
            where={
                'mocktestid': 1583689592,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
        include: Optional[types.mocktestsInclude] = None,
        order: Optional[Union[types.mocktestsOrderByInput, List[types.mocktestsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple mocktests records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of mocktests records returned
        skip
            Ignore the first N results
        where
            mocktests filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktests model
        order
            Order the returned mocktests records by any field
        distinct
            Filter mocktests records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.mocktests]
            The list of all mocktests records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 mocktests records
        mocktests = mocktests.prisma().find_many(take=10)

        # find the first 5 mocktests records ordered by the examtypeid field
        mocktests = mocktests.prisma().find_many(
            take=5,
            order={
                'examtypeid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
        include: Optional[types.mocktestsInclude] = None,
        order: Optional[Union[types.mocktestsOrderByInput, List[types.mocktestsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single mocktests record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktests filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktests model
        order
            Order the returned mocktests records by any field
        distinct
            Filter mocktests records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktests
            The first mocktests record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktests record ordered by the date field
        mocktests = mocktests.prisma().find_first(
            skip=1,
            order={
                'date': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
        include: Optional[types.mocktestsInclude] = None,
        order: Optional[Union[types.mocktestsOrderByInput, List[types.mocktestsOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single mocktests record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktests filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktests model
        order
            Order the returned mocktests records by any field
        distinct
            Filter mocktests records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktests
            The first mocktests record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktests record ordered by the starttime field
        mocktests = mocktests.prisma().find_first_or_raise(
            skip=1,
            order={
                'starttime': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.mocktestsUpdateInput,
        where: types.mocktestsWhereUniqueInput,
        include: Optional[types.mocktestsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single mocktests record.

        Parameters
        ----------
        data
            mocktests record data specifying what to update
        where
            mocktests filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The updated mocktests record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        mocktests = mocktests.prisma().update(
            where={
                'mocktestid': 878442065,
            },
            data={
                # data to update the mocktests record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.mocktestsWhereUniqueInput,
        data: types.mocktestsUpsertInput,
        include: Optional[types.mocktestsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            mocktests filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned mocktests model

        Returns
        -------
        prisma.models.mocktests
            The created or updated mocktests record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktests = mocktests.prisma().upsert(
            where={
                'mocktestid': 1675280054,
            },
            data={
                'create': {
                    'mocktestid': 1675280054,
                    'date': datetime.datetime.utcnow(),
                    'starttime': datetime.datetime.utcnow(),
                    'endtime': datetime.datetime.utcnow(),
                },
                'update': {
                    'date': datetime.datetime.utcnow(),
                    'starttime': datetime.datetime.utcnow(),
                    'endtime': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.mocktestsUpdateManyMutationInput,
        where: types.mocktestsWhereInput,
    ) -> int:
        """Update multiple mocktests records

        Parameters
        ----------
        data
            mocktests data to update the selected mocktests records to
        where
            Filter to select the mocktests records to update

        Returns
        -------
        int
            The total number of mocktests records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all mocktests records
        total = mocktests.prisma().update_many(
            data={
                'endtime': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of mocktests records present in the database

        Parameters
        ----------
        select
            Select the mocktests fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktests filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktests.prisma().count()

        # results: prisma.types.mocktestsCountAggregateOutput
        results = mocktests.prisma().count(
            select={
                '_all': True,
                'isactive': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.mocktestsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
    ) -> types.mocktestsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.mocktestsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsWhereInput] = None,
        cursor: Optional[types.mocktestsWhereUniqueInput] = None,
    ) -> Union[int, types.mocktestsCountAggregateOutput]:
        """Count the number of mocktests records present in the database

        Parameters
        ----------
        select
            Select the mocktests fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktests filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktests.prisma().count()

        # results: prisma.types.mocktestsCountAggregateOutput
        results = mocktests.prisma().count(
            select={
                '_all': True,
                'mocktestid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.mocktestsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.mocktestsWhereInput] = None
    ) -> int:
        """Delete multiple mocktests records.

        Parameters
        ----------
        where
            Optional mocktests filter to find the records to be deleted

        Returns
        -------
        int
            The total number of mocktests records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all mocktests records
        total = mocktests.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.mocktestsScalarFieldKeys'],
        *,
        where: Optional['types.mocktestsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.mocktestsAvgAggregateInput'] = None,
        sum: Optional['types.mocktestsSumAggregateInput'] = None,
        min: Optional['types.mocktestsMinAggregateInput'] = None,
        max: Optional['types.mocktestsMaxAggregateInput'] = None,
        having: Optional['types.mocktestsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.mocktestsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.mocktestsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.mocktestsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.mocktestsGroupByOutput']:
        """Group mocktests records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar mocktests fields to group records by
        where
            mocktests filter to select records
        take
            Limit the maximum number of mocktests records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.mocktestsGroupByOutput]
            A list of dictionaries representing the mocktests record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group mocktests records by difficulty values
        # and count how many records are in each group
        results = mocktests.prisma().group_by(
            ['difficulty'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class mocktestsectionscoresActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.mocktestsectionscores]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = mocktestsectionscores.prisma().query_raw(
            'SELECT * FROM mocktestsectionscores WHERE scoreid = $1',
            1627576247,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.mocktestsectionscores
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = mocktestsectionscores.prisma().query_first(
            'SELECT * FROM mocktestsectionscores WHERE attemptid = $1',
            2054802212,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.mocktestsectionscoresCreateInput,
        include: Optional[types.mocktestsectionscoresInclude] = None
    ) -> _PrismaModelT:
        """Create a new mocktestsectionscores record.

        Parameters
        ----------
        data
            mocktestsectionscores record data
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The created mocktestsectionscores record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a mocktestsectionscores record from just the required fields
        mocktestsectionscores = mocktestsectionscores.prisma().create(
            data={
                # data to create a mocktestsectionscores record
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.mocktestsectionscoresCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple mocktestsectionscores records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of mocktestsectionscores record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = mocktestsectionscores.prisma().create_many(
            data=[
                {
                    # data to create a mocktestsectionscores record
                },
                {
                    # data to create a mocktestsectionscores record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.mocktestsectionscoresWhereUniqueInput,
        include: Optional[types.mocktestsectionscoresInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single mocktestsectionscores record.

        Parameters
        ----------
        where
            mocktestsectionscores filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The deleted mocktestsectionscores record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestsectionscores = mocktestsectionscores.prisma().delete(
            where={
                'scoreid': 60335757,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.mocktestsectionscoresWhereUniqueInput,
        include: Optional[types.mocktestsectionscoresInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique mocktestsectionscores record.

        Parameters
        ----------
        where
            mocktestsectionscores filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The found mocktestsectionscores record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestsectionscores = mocktestsectionscores.prisma().find_unique(
            where={
                'scoreid': 684462146,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.mocktestsectionscoresWhereUniqueInput,
        include: Optional[types.mocktestsectionscoresInclude] = None
    ) -> _PrismaModelT:
        """Find a unique mocktestsectionscores record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            mocktestsectionscores filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The found mocktestsectionscores record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestsectionscores = mocktestsectionscores.prisma().find_unique_or_raise(
            where={
                'scoreid': 1625503827,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
        include: Optional[types.mocktestsectionscoresInclude] = None,
        order: Optional[Union[types.mocktestsectionscoresOrderByInput, List[types.mocktestsectionscoresOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsectionscoresScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple mocktestsectionscores records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of mocktestsectionscores records returned
        skip
            Ignore the first N results
        where
            mocktestsectionscores filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model
        order
            Order the returned mocktestsectionscores records by any field
        distinct
            Filter mocktestsectionscores records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.mocktestsectionscores]
            The list of all mocktestsectionscores records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 mocktestsectionscores records
        mocktestsectionscores = mocktestsectionscores.prisma().find_many(take=10)

        # find the first 5 mocktestsectionscores records ordered by the sectionid field
        mocktestsectionscores = mocktestsectionscores.prisma().find_many(
            take=5,
            order={
                'sectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
        include: Optional[types.mocktestsectionscoresInclude] = None,
        order: Optional[Union[types.mocktestsectionscoresOrderByInput, List[types.mocktestsectionscoresOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsectionscoresScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single mocktestsectionscores record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktestsectionscores filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model
        order
            Order the returned mocktestsectionscores records by any field
        distinct
            Filter mocktestsectionscores records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktestsectionscores
            The first mocktestsectionscores record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktestsectionscores record ordered by the score field
        mocktestsectionscores = mocktestsectionscores.prisma().find_first(
            skip=1,
            order={
                'score': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
        include: Optional[types.mocktestsectionscoresInclude] = None,
        order: Optional[Union[types.mocktestsectionscoresOrderByInput, List[types.mocktestsectionscoresOrderByInput]]] = None,
        distinct: Optional[List[types.mocktestsectionscoresScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single mocktestsectionscores record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocktestsectionscores filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model
        order
            Order the returned mocktestsectionscores records by any field
        distinct
            Filter mocktestsectionscores records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocktestsectionscores
            The first mocktestsectionscores record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocktestsectionscores record ordered by the scoreid field
        mocktestsectionscores = mocktestsectionscores.prisma().find_first_or_raise(
            skip=1,
            order={
                'scoreid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.mocktestsectionscoresUpdateInput,
        where: types.mocktestsectionscoresWhereUniqueInput,
        include: Optional[types.mocktestsectionscoresInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single mocktestsectionscores record.

        Parameters
        ----------
        data
            mocktestsectionscores record data specifying what to update
        where
            mocktestsectionscores filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The updated mocktestsectionscores record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        mocktestsectionscores = mocktestsectionscores.prisma().update(
            where={
                'scoreid': 521827728,
            },
            data={
                # data to update the mocktestsectionscores record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.mocktestsectionscoresWhereUniqueInput,
        data: types.mocktestsectionscoresUpsertInput,
        include: Optional[types.mocktestsectionscoresInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            mocktestsectionscores filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned mocktestsectionscores model

        Returns
        -------
        prisma.models.mocktestsectionscores
            The created or updated mocktestsectionscores record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocktestsectionscores = mocktestsectionscores.prisma().upsert(
            where={
                'scoreid': 1266032265,
            },
            data={
                'create': {
                    'scoreid': 1266032265,
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.mocktestsectionscoresUpdateManyMutationInput,
        where: types.mocktestsectionscoresWhereInput,
    ) -> int:
        """Update multiple mocktestsectionscores records

        Parameters
        ----------
        data
            mocktestsectionscores data to update the selected mocktestsectionscores records to
        where
            Filter to select the mocktestsectionscores records to update

        Returns
        -------
        int
            The total number of mocktestsectionscores records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all mocktestsectionscores records
        total = mocktestsectionscores.prisma().update_many(
            data={
                'attemptid': 93253262
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
    ) -> int:
        """Count the number of mocktestsectionscores records present in the database

        Parameters
        ----------
        select
            Select the mocktestsectionscores fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktestsectionscores filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestsectionscoresCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktestsectionscores.prisma().count()

        # results: prisma.types.mocktestsectionscoresCountAggregateOutput
        results = mocktestsectionscores.prisma().count(
            select={
                '_all': True,
                'sectionid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.mocktestsectionscoresCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
    ) -> types.mocktestsectionscoresCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.mocktestsectionscoresCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocktestsectionscoresWhereInput] = None,
        cursor: Optional[types.mocktestsectionscoresWhereUniqueInput] = None,
    ) -> Union[int, types.mocktestsectionscoresCountAggregateOutput]:
        """Count the number of mocktestsectionscores records present in the database

        Parameters
        ----------
        select
            Select the mocktestsectionscores fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocktestsectionscores filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocktestsectionscoresCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocktestsectionscores.prisma().count()

        # results: prisma.types.mocktestsectionscoresCountAggregateOutput
        results = mocktestsectionscores.prisma().count(
            select={
                '_all': True,
                'score': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.mocktestsectionscoresCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.mocktestsectionscoresWhereInput] = None
    ) -> int:
        """Delete multiple mocktestsectionscores records.

        Parameters
        ----------
        where
            Optional mocktestsectionscores filter to find the records to be deleted

        Returns
        -------
        int
            The total number of mocktestsectionscores records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all mocktestsectionscores records
        total = mocktestsectionscores.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.mocktestsectionscoresScalarFieldKeys'],
        *,
        where: Optional['types.mocktestsectionscoresWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.mocktestsectionscoresAvgAggregateInput'] = None,
        sum: Optional['types.mocktestsectionscoresSumAggregateInput'] = None,
        min: Optional['types.mocktestsectionscoresMinAggregateInput'] = None,
        max: Optional['types.mocktestsectionscoresMaxAggregateInput'] = None,
        having: Optional['types.mocktestsectionscoresScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.mocktestsectionscoresCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.mocktestsectionscoresScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.mocktestsectionscoresScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.mocktestsectionscoresGroupByOutput']:
        """Group mocktestsectionscores records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar mocktestsectionscores fields to group records by
        where
            mocktestsectionscores filter to select records
        take
            Limit the maximum number of mocktestsectionscores records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.mocktestsectionscoresGroupByOutput]
            A list of dictionaries representing the mocktestsectionscores record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group mocktestsectionscores records by scoreid values
        # and count how many records are in each group
        results = mocktestsectionscores.prisma().group_by(
            ['scoreid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class mocksectionsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.mocksections]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = mocksections.prisma().query_raw(
            'SELECT * FROM mocksections WHERE mocksectionid = $1',
            2053047983,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.mocksections
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = mocksections.prisma().query_first(
            'SELECT * FROM mocksections WHERE mocktestid = $1',
            685333180,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.mocksectionsCreateInput,
        include: Optional[types.mocksectionsInclude] = None
    ) -> _PrismaModelT:
        """Create a new mocksections record.

        Parameters
        ----------
        data
            mocksections record data
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The created mocksections record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a mocksections record from just the required fields
        mocksections = mocksections.prisma().create(
            data={
                # data to create a mocksections record
                'mocktestid': 127474245,
                'sectionnumber': 948921754,
                'sectionid': 1964990155,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.mocksectionsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple mocksections records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of mocksections record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = mocksections.prisma().create_many(
            data=[
                {
                    # data to create a mocksections record
                    'mocktestid': 1228891816,
                    'sectionnumber': 255202753,
                    'sectionid': 1223573862,
                },
                {
                    # data to create a mocksections record
                    'mocktestid': 541269159,
                    'sectionnumber': 1064846676,
                    'sectionid': 508382461,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.mocksectionsWhereUniqueInput,
        include: Optional[types.mocksectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single mocksections record.

        Parameters
        ----------
        where
            mocksections filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The deleted mocksections record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocksections = mocksections.prisma().delete(
            where={
                'mocksectionid': 1024265714,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.mocksectionsWhereUniqueInput,
        include: Optional[types.mocksectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique mocksections record.

        Parameters
        ----------
        where
            mocksections filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The found mocksections record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocksections = mocksections.prisma().find_unique(
            where={
                'mocksectionid': 872078403,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.mocksectionsWhereUniqueInput,
        include: Optional[types.mocksectionsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique mocksections record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            mocksections filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The found mocksections record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocksections = mocksections.prisma().find_unique_or_raise(
            where={
                'mocksectionid': 1874748096,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
        include: Optional[types.mocksectionsInclude] = None,
        order: Optional[Union[types.mocksectionsOrderByInput, List[types.mocksectionsOrderByInput]]] = None,
        distinct: Optional[List[types.mocksectionsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple mocksections records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of mocksections records returned
        skip
            Ignore the first N results
        where
            mocksections filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocksections model
        order
            Order the returned mocksections records by any field
        distinct
            Filter mocksections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.mocksections]
            The list of all mocksections records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 mocksections records
        mocksections = mocksections.prisma().find_many(take=10)

        # find the first 5 mocksections records ordered by the sectionnumber field
        mocksections = mocksections.prisma().find_many(
            take=5,
            order={
                'sectionnumber': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
        include: Optional[types.mocksectionsInclude] = None,
        order: Optional[Union[types.mocksectionsOrderByInput, List[types.mocksectionsOrderByInput]]] = None,
        distinct: Optional[List[types.mocksectionsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single mocksections record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocksections filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocksections model
        order
            Order the returned mocksections records by any field
        distinct
            Filter mocksections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocksections
            The first mocksections record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocksections record ordered by the sectionid field
        mocksections = mocksections.prisma().find_first(
            skip=1,
            order={
                'sectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
        include: Optional[types.mocksectionsInclude] = None,
        order: Optional[Union[types.mocksectionsOrderByInput, List[types.mocksectionsOrderByInput]]] = None,
        distinct: Optional[List[types.mocksectionsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single mocksections record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            mocksections filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned mocksections model
        order
            Order the returned mocksections records by any field
        distinct
            Filter mocksections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.mocksections
            The first mocksections record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second mocksections record ordered by the mocksectionid field
        mocksections = mocksections.prisma().find_first_or_raise(
            skip=1,
            order={
                'mocksectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.mocksectionsUpdateInput,
        where: types.mocksectionsWhereUniqueInput,
        include: Optional[types.mocksectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single mocksections record.

        Parameters
        ----------
        data
            mocksections record data specifying what to update
        where
            mocksections filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The updated mocksections record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        mocksections = mocksections.prisma().update(
            where={
                'mocksectionid': 916896761,
            },
            data={
                # data to update the mocksections record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.mocksectionsWhereUniqueInput,
        data: types.mocksectionsUpsertInput,
        include: Optional[types.mocksectionsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            mocksections filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned mocksections model

        Returns
        -------
        prisma.models.mocksections
            The created or updated mocksections record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        mocksections = mocksections.prisma().upsert(
            where={
                'mocksectionid': 769267518,
            },
            data={
                'create': {
                    'mocksectionid': 769267518,
                    'mocktestid': 541269159,
                    'sectionnumber': 1064846676,
                    'sectionid': 508382461,
                },
                'update': {
                    'mocktestid': 541269159,
                    'sectionnumber': 1064846676,
                    'sectionid': 508382461,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.mocksectionsUpdateManyMutationInput,
        where: types.mocksectionsWhereInput,
    ) -> int:
        """Update multiple mocksections records

        Parameters
        ----------
        data
            mocksections data to update the selected mocksections records to
        where
            Filter to select the mocksections records to update

        Returns
        -------
        int
            The total number of mocksections records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all mocksections records
        total = mocksections.prisma().update_many(
            data={
                'mocktestid': 820312479
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of mocksections records present in the database

        Parameters
        ----------
        select
            Select the mocksections fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocksections filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocksectionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocksections.prisma().count()

        # results: prisma.types.mocksectionsCountAggregateOutput
        results = mocksections.prisma().count(
            select={
                '_all': True,
                'sectionnumber': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.mocksectionsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
    ) -> types.mocksectionsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.mocksectionsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.mocksectionsWhereInput] = None,
        cursor: Optional[types.mocksectionsWhereUniqueInput] = None,
    ) -> Union[int, types.mocksectionsCountAggregateOutput]:
        """Count the number of mocksections records present in the database

        Parameters
        ----------
        select
            Select the mocksections fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            mocksections filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.mocksectionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = mocksections.prisma().count()

        # results: prisma.types.mocksectionsCountAggregateOutput
        results = mocksections.prisma().count(
            select={
                '_all': True,
                'sectionid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.mocksectionsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.mocksectionsWhereInput] = None
    ) -> int:
        """Delete multiple mocksections records.

        Parameters
        ----------
        where
            Optional mocksections filter to find the records to be deleted

        Returns
        -------
        int
            The total number of mocksections records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all mocksections records
        total = mocksections.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.mocksectionsScalarFieldKeys'],
        *,
        where: Optional['types.mocksectionsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.mocksectionsAvgAggregateInput'] = None,
        sum: Optional['types.mocksectionsSumAggregateInput'] = None,
        min: Optional['types.mocksectionsMinAggregateInput'] = None,
        max: Optional['types.mocksectionsMaxAggregateInput'] = None,
        having: Optional['types.mocksectionsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.mocksectionsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.mocksectionsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.mocksectionsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.mocksectionsGroupByOutput']:
        """Group mocksections records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar mocksections fields to group records by
        where
            mocksections filter to select records
        take
            Limit the maximum number of mocksections records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.mocksectionsGroupByOutput]
            A list of dictionaries representing the mocksections record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group mocksections records by mocksectionid values
        # and count how many records are in each group
        results = mocksections.prisma().group_by(
            ['mocksectionid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class performanceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.performance]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = performance.prisma().query_raw(
            'SELECT * FROM performance WHERE userid = $1',
            92728044,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.performance
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = performance.prisma().query_first(
            'SELECT * FROM performance WHERE sectionid = $1',
            344858293,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.performanceCreateInput,
        include: Optional[types.performanceInclude] = None
    ) -> _PrismaModelT:
        """Create a new performance record.

        Parameters
        ----------
        data
            performance record data
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The created performance record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a performance record from just the required fields
        performance = performance.prisma().create(
            data={
                # data to create a performance record
                'userid': 1121741130,
                'sectionid': 1495896251,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.performanceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple performance records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of performance record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = performance.prisma().create_many(
            data=[
                {
                    # data to create a performance record
                    'userid': 208521688,
                    'sectionid': 860811569,
                },
                {
                    # data to create a performance record
                    'userid': 1660932118,
                    'sectionid': 525761943,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.performanceWhereUniqueInput,
        include: Optional[types.performanceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single performance record.

        Parameters
        ----------
        where
            performance filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The deleted performance record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        performance = performance.prisma().delete(
            where={
                # performance where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.performanceWhereUniqueInput,
        include: Optional[types.performanceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique performance record.

        Parameters
        ----------
        where
            performance filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The found performance record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        performance = performance.prisma().find_unique(
            where={
                # performance where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.performanceWhereUniqueInput,
        include: Optional[types.performanceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique performance record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            performance filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The found performance record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        performance = performance.prisma().find_unique_or_raise(
            where={
                # performance where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
        include: Optional[types.performanceInclude] = None,
        order: Optional[Union[types.performanceOrderByInput, List[types.performanceOrderByInput]]] = None,
        distinct: Optional[List[types.performanceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple performance records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of performance records returned
        skip
            Ignore the first N results
        where
            performance filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned performance model
        order
            Order the returned performance records by any field
        distinct
            Filter performance records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.performance]
            The list of all performance records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 performance records
        performances = performance.prisma().find_many(take=10)

        # find the first 5 performance records ordered by the correctcount field
        performances = performance.prisma().find_many(
            take=5,
            order={
                'correctcount': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
        include: Optional[types.performanceInclude] = None,
        order: Optional[Union[types.performanceOrderByInput, List[types.performanceOrderByInput]]] = None,
        distinct: Optional[List[types.performanceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single performance record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            performance filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned performance model
        order
            Order the returned performance records by any field
        distinct
            Filter performance records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.performance
            The first performance record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second performance record ordered by the incorrectcount field
        performance = performance.prisma().find_first(
            skip=1,
            order={
                'incorrectcount': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
        include: Optional[types.performanceInclude] = None,
        order: Optional[Union[types.performanceOrderByInput, List[types.performanceOrderByInput]]] = None,
        distinct: Optional[List[types.performanceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single performance record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            performance filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned performance model
        order
            Order the returned performance records by any field
        distinct
            Filter performance records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.performance
            The first performance record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second performance record ordered by the partialcorrectcount field
        performance = performance.prisma().find_first_or_raise(
            skip=1,
            order={
                'partialcorrectcount': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.performanceUpdateInput,
        where: types.performanceWhereUniqueInput,
        include: Optional[types.performanceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single performance record.

        Parameters
        ----------
        data
            performance record data specifying what to update
        where
            performance filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The updated performance record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        performance = performance.prisma().update(
            where={
                # performance where unique filter

            },
            data={
                # data to update the performance record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.performanceWhereUniqueInput,
        data: types.performanceUpsertInput,
        include: Optional[types.performanceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            performance filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned performance model

        Returns
        -------
        prisma.models.performance
            The created or updated performance record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        performance = performance.prisma().upsert(
            where={
                # performance where unique filter
            },
            data={
                'create': {
                    # performance data to be set if the record does not exist
                },
                'update': {
                    # performance data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.performanceUpdateManyMutationInput,
        where: types.performanceWhereInput,
    ) -> int:
        """Update multiple performance records

        Parameters
        ----------
        data
            performance data to update the selected performance records to
        where
            Filter to select the performance records to update

        Returns
        -------
        int
            The total number of performance records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all performance records
        total = performance.prisma().update_many(
            data={
                'userid': 736209796
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of performance records present in the database

        Parameters
        ----------
        select
            Select the performance fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            performance filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.performanceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = performance.prisma().count()

        # results: prisma.types.performanceCountAggregateOutput
        results = performance.prisma().count(
            select={
                '_all': True,
                'sectionid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.performanceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
    ) -> types.performanceCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.performanceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.performanceWhereInput] = None,
        cursor: Optional[types.performanceWhereUniqueInput] = None,
    ) -> Union[int, types.performanceCountAggregateOutput]:
        """Count the number of performance records present in the database

        Parameters
        ----------
        select
            Select the performance fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            performance filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.performanceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = performance.prisma().count()

        # results: prisma.types.performanceCountAggregateOutput
        results = performance.prisma().count(
            select={
                '_all': True,
                'correctcount': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.performanceCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.performanceWhereInput] = None
    ) -> int:
        """Delete multiple performance records.

        Parameters
        ----------
        where
            Optional performance filter to find the records to be deleted

        Returns
        -------
        int
            The total number of performance records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all performance records
        total = performance.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.performanceScalarFieldKeys'],
        *,
        where: Optional['types.performanceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.performanceAvgAggregateInput'] = None,
        sum: Optional['types.performanceSumAggregateInput'] = None,
        min: Optional['types.performanceMinAggregateInput'] = None,
        max: Optional['types.performanceMaxAggregateInput'] = None,
        having: Optional['types.performanceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.performanceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.performanceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.performanceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.performanceGroupByOutput']:
        """Group performance records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar performance fields to group records by
        where
            performance filter to select records
        take
            Limit the maximum number of performance records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.performanceGroupByOutput]
            A list of dictionaries representing the performance record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group performance records by incorrectcount values
        # and count how many records are in each group
        results = performance.prisma().group_by(
            ['incorrectcount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class problemoptionsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.problemoptions]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = problemoptions.prisma().query_raw(
            'SELECT * FROM problemoptions WHERE optionid = $1',
            493907821,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.problemoptions
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = problemoptions.prisma().query_first(
            'SELECT * FROM problemoptions WHERE problemid = $1',
            639686562,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.problemoptionsCreateInput,
        include: Optional[types.problemoptionsInclude] = None
    ) -> _PrismaModelT:
        """Create a new problemoptions record.

        Parameters
        ----------
        data
            problemoptions record data
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The created problemoptions record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a problemoptions record from just the required fields
        problemoptions = problemoptions.prisma().create(
            data={
                # data to create a problemoptions record
                'optiontext': 'gfeaahdeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.problemoptionsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple problemoptions records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of problemoptions record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = problemoptions.prisma().create_many(
            data=[
                {
                    # data to create a problemoptions record
                    'optiontext': 'bjafcgbffc',
                },
                {
                    # data to create a problemoptions record
                    'optiontext': 'hihegjif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.problemoptionsWhereUniqueInput,
        include: Optional[types.problemoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single problemoptions record.

        Parameters
        ----------
        where
            problemoptions filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The deleted problemoptions record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemoptions = problemoptions.prisma().delete(
            where={
                'optionid': 1398328302,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.problemoptionsWhereUniqueInput,
        include: Optional[types.problemoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique problemoptions record.

        Parameters
        ----------
        where
            problemoptions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The found problemoptions record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemoptions = problemoptions.prisma().find_unique(
            where={
                'optionid': 856000655,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.problemoptionsWhereUniqueInput,
        include: Optional[types.problemoptionsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique problemoptions record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            problemoptions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The found problemoptions record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemoptions = problemoptions.prisma().find_unique_or_raise(
            where={
                'optionid': 1452336924,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
        include: Optional[types.problemoptionsInclude] = None,
        order: Optional[Union[types.problemoptionsOrderByInput, List[types.problemoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.problemoptionsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple problemoptions records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of problemoptions records returned
        skip
            Ignore the first N results
        where
            problemoptions filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemoptions model
        order
            Order the returned problemoptions records by any field
        distinct
            Filter problemoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.problemoptions]
            The list of all problemoptions records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 problemoptions records
        problemoptions = problemoptions.prisma().find_many(take=10)

        # find the first 5 problemoptions records ordered by the optiontext field
        problemoptions = problemoptions.prisma().find_many(
            take=5,
            order={
                'optiontext': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
        include: Optional[types.problemoptionsInclude] = None,
        order: Optional[Union[types.problemoptionsOrderByInput, List[types.problemoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.problemoptionsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single problemoptions record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemoptions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemoptions model
        order
            Order the returned problemoptions records by any field
        distinct
            Filter problemoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemoptions
            The first problemoptions record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemoptions record ordered by the iscorrect field
        problemoptions = problemoptions.prisma().find_first(
            skip=1,
            order={
                'iscorrect': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
        include: Optional[types.problemoptionsInclude] = None,
        order: Optional[Union[types.problemoptionsOrderByInput, List[types.problemoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.problemoptionsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single problemoptions record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemoptions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemoptions model
        order
            Order the returned problemoptions records by any field
        distinct
            Filter problemoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemoptions
            The first problemoptions record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemoptions record ordered by the group field
        problemoptions = problemoptions.prisma().find_first_or_raise(
            skip=1,
            order={
                'group': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.problemoptionsUpdateInput,
        where: types.problemoptionsWhereUniqueInput,
        include: Optional[types.problemoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single problemoptions record.

        Parameters
        ----------
        data
            problemoptions record data specifying what to update
        where
            problemoptions filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The updated problemoptions record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        problemoptions = problemoptions.prisma().update(
            where={
                'optionid': 1573199653,
            },
            data={
                # data to update the problemoptions record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.problemoptionsWhereUniqueInput,
        data: types.problemoptionsUpsertInput,
        include: Optional[types.problemoptionsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            problemoptions filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned problemoptions model

        Returns
        -------
        prisma.models.problemoptions
            The created or updated problemoptions record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemoptions = problemoptions.prisma().upsert(
            where={
                'optionid': 2013903098,
            },
            data={
                'create': {
                    'optionid': 2013903098,
                    'optiontext': 'hihegjif',
                },
                'update': {
                    'optiontext': 'hihegjif',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.problemoptionsUpdateManyMutationInput,
        where: types.problemoptionsWhereInput,
    ) -> int:
        """Update multiple problemoptions records

        Parameters
        ----------
        data
            problemoptions data to update the selected problemoptions records to
        where
            Filter to select the problemoptions records to update

        Returns
        -------
        int
            The total number of problemoptions records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all problemoptions records
        total = problemoptions.prisma().update_many(
            data={
                'optionid': 500965035
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of problemoptions records present in the database

        Parameters
        ----------
        select
            Select the problemoptions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemoptions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemoptionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemoptions.prisma().count()

        # results: prisma.types.problemoptionsCountAggregateOutput
        results = problemoptions.prisma().count(
            select={
                '_all': True,
                'problemid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.problemoptionsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
    ) -> types.problemoptionsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.problemoptionsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemoptionsWhereInput] = None,
        cursor: Optional[types.problemoptionsWhereUniqueInput] = None,
    ) -> Union[int, types.problemoptionsCountAggregateOutput]:
        """Count the number of problemoptions records present in the database

        Parameters
        ----------
        select
            Select the problemoptions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemoptions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemoptionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemoptions.prisma().count()

        # results: prisma.types.problemoptionsCountAggregateOutput
        results = problemoptions.prisma().count(
            select={
                '_all': True,
                'optiontext': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.problemoptionsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.problemoptionsWhereInput] = None
    ) -> int:
        """Delete multiple problemoptions records.

        Parameters
        ----------
        where
            Optional problemoptions filter to find the records to be deleted

        Returns
        -------
        int
            The total number of problemoptions records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all problemoptions records
        total = problemoptions.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.problemoptionsScalarFieldKeys'],
        *,
        where: Optional['types.problemoptionsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.problemoptionsAvgAggregateInput'] = None,
        sum: Optional['types.problemoptionsSumAggregateInput'] = None,
        min: Optional['types.problemoptionsMinAggregateInput'] = None,
        max: Optional['types.problemoptionsMaxAggregateInput'] = None,
        having: Optional['types.problemoptionsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.problemoptionsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.problemoptionsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.problemoptionsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.problemoptionsGroupByOutput']:
        """Group problemoptions records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar problemoptions fields to group records by
        where
            problemoptions filter to select records
        take
            Limit the maximum number of problemoptions records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.problemoptionsGroupByOutput]
            A list of dictionaries representing the problemoptions record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group problemoptions records by iscorrect values
        # and count how many records are in each group
        results = problemoptions.prisma().group_by(
            ['iscorrect'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class problemsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.problems]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = problems.prisma().query_raw(
            'SELECT * FROM problems WHERE type = $1',
            'biaagcedjc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.problems
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = problems.prisma().query_first(
            'SELECT * FROM problems WHERE prompt = $1',
            'cahhaghecf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.problemsCreateInput,
        include: Optional[types.problemsInclude] = None
    ) -> _PrismaModelT:
        """Create a new problems record.

        Parameters
        ----------
        data
            problems record data
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The created problems record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a problems record from just the required fields
        problems = problems.prisma().create(
            data={
                # data to create a problems record
                'title': 'bghcbbcidi',
                'text': 'jcgghhgdj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.problemsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple problems records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of problems record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = problems.prisma().create_many(
            data=[
                {
                    # data to create a problems record
                    'title': 'beehgcebbg',
                    'text': 'bhdiaidiaf',
                },
                {
                    # data to create a problems record
                    'title': 'deajegcfi',
                    'text': 'gabahhhjf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.problemsWhereUniqueInput,
        include: Optional[types.problemsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single problems record.

        Parameters
        ----------
        where
            problems filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The deleted problems record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problems = problems.prisma().delete(
            where={
                'problemid': 290603296,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.problemsWhereUniqueInput,
        include: Optional[types.problemsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique problems record.

        Parameters
        ----------
        where
            problems filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The found problems record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problems = problems.prisma().find_unique(
            where={
                'problemid': 1855826649,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.problemsWhereUniqueInput,
        include: Optional[types.problemsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique problems record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            problems filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The found problems record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problems = problems.prisma().find_unique_or_raise(
            where={
                'problemid': 1611009182,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
        include: Optional[types.problemsInclude] = None,
        order: Optional[Union[types.problemsOrderByInput, List[types.problemsOrderByInput]]] = None,
        distinct: Optional[List[types.problemsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple problems records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of problems records returned
        skip
            Ignore the first N results
        where
            problems filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problems model
        order
            Order the returned problems records by any field
        distinct
            Filter problems records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.problems]
            The list of all problems records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 problems records
        problems = problems.prisma().find_many(take=10)

        # find the first 5 problems records ordered by the problemid field
        problems = problems.prisma().find_many(
            take=5,
            order={
                'problemid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
        include: Optional[types.problemsInclude] = None,
        order: Optional[Union[types.problemsOrderByInput, List[types.problemsOrderByInput]]] = None,
        distinct: Optional[List[types.problemsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single problems record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problems filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problems model
        order
            Order the returned problems records by any field
        distinct
            Filter problems records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problems
            The first problems record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problems record ordered by the sectionid field
        problems = problems.prisma().find_first(
            skip=1,
            order={
                'sectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
        include: Optional[types.problemsInclude] = None,
        order: Optional[Union[types.problemsOrderByInput, List[types.problemsOrderByInput]]] = None,
        distinct: Optional[List[types.problemsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single problems record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problems filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problems model
        order
            Order the returned problems records by any field
        distinct
            Filter problems records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problems
            The first problems record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problems record ordered by the examtypeid field
        problems = problems.prisma().find_first_or_raise(
            skip=1,
            order={
                'examtypeid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.problemsUpdateInput,
        where: types.problemsWhereUniqueInput,
        include: Optional[types.problemsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single problems record.

        Parameters
        ----------
        data
            problems record data specifying what to update
        where
            problems filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The updated problems record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        problems = problems.prisma().update(
            where={
                'problemid': 446673791,
            },
            data={
                # data to update the problems record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.problemsWhereUniqueInput,
        data: types.problemsUpsertInput,
        include: Optional[types.problemsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            problems filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned problems model

        Returns
        -------
        prisma.models.problems
            The created or updated problems record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problems = problems.prisma().upsert(
            where={
                'problemid': 300568396,
            },
            data={
                'create': {
                    'problemid': 300568396,
                    'title': 'deajegcfi',
                    'text': 'gabahhhjf',
                },
                'update': {
                    'title': 'deajegcfi',
                    'text': 'gabahhhjf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.problemsUpdateManyMutationInput,
        where: types.problemsWhereInput,
    ) -> int:
        """Update multiple problems records

        Parameters
        ----------
        data
            problems data to update the selected problems records to
        where
            Filter to select the problems records to update

        Returns
        -------
        int
            The total number of problems records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all problems records
        total = problems.prisma().update_many(
            data={
                'title': 'gdcgcgagj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of problems records present in the database

        Parameters
        ----------
        select
            Select the problems fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problems filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problems.prisma().count()

        # results: prisma.types.problemsCountAggregateOutput
        results = problems.prisma().count(
            select={
                '_all': True,
                'text': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.problemsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
    ) -> types.problemsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.problemsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemsWhereInput] = None,
        cursor: Optional[types.problemsWhereUniqueInput] = None,
    ) -> Union[int, types.problemsCountAggregateOutput]:
        """Count the number of problems records present in the database

        Parameters
        ----------
        select
            Select the problems fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problems filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problems.prisma().count()

        # results: prisma.types.problemsCountAggregateOutput
        results = problems.prisma().count(
            select={
                '_all': True,
                'difficulty': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.problemsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.problemsWhereInput] = None
    ) -> int:
        """Delete multiple problems records.

        Parameters
        ----------
        where
            Optional problems filter to find the records to be deleted

        Returns
        -------
        int
            The total number of problems records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all problems records
        total = problems.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.problemsScalarFieldKeys'],
        *,
        where: Optional['types.problemsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.problemsAvgAggregateInput'] = None,
        sum: Optional['types.problemsSumAggregateInput'] = None,
        min: Optional['types.problemsMinAggregateInput'] = None,
        max: Optional['types.problemsMaxAggregateInput'] = None,
        having: Optional['types.problemsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.problemsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.problemsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.problemsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.problemsGroupByOutput']:
        """Group problems records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar problems fields to group records by
        where
            problems filter to select records
        take
            Limit the maximum number of problems records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.problemsGroupByOutput]
            A list of dictionaries representing the problems record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group problems records by correctattemptscount values
        # and count how many records are in each group
        results = problems.prisma().group_by(
            ['correctattemptscount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class problemssettagsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.problemssettags]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = problemssettags.prisma().query_raw(
            'SELECT * FROM problemssettags WHERE problemsSetId = $1',
            1724011690,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.problemssettags
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = problemssettags.prisma().query_first(
            'SELECT * FROM problemssettags WHERE tagid = $1',
            470157467,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.problemssettagsCreateInput,
        include: Optional[types.problemssettagsInclude] = None
    ) -> _PrismaModelT:
        """Create a new problemssettags record.

        Parameters
        ----------
        data
            problemssettags record data
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The created problemssettags record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a problemssettags record from just the required fields
        problemssettags = problemssettags.prisma().create(
            data={
                # data to create a problemssettags record
                'problemsSetId': 1209209912,
                'tagid': 1536744465,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.problemssettagsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple problemssettags records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of problemssettags record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = problemssettags.prisma().create_many(
            data=[
                {
                    # data to create a problemssettags record
                    'problemsSetId': 424218998,
                    'tagid': 2125632375,
                },
                {
                    # data to create a problemssettags record
                    'problemsSetId': 536951780,
                    'tagid': 924723277,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.problemssettagsWhereUniqueInput,
        include: Optional[types.problemssettagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single problemssettags record.

        Parameters
        ----------
        where
            problemssettags filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The deleted problemssettags record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemssettags = problemssettags.prisma().delete(
            where={
                # problemssettags where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.problemssettagsWhereUniqueInput,
        include: Optional[types.problemssettagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique problemssettags record.

        Parameters
        ----------
        where
            problemssettags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The found problemssettags record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemssettags = problemssettags.prisma().find_unique(
            where={
                # problemssettags where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.problemssettagsWhereUniqueInput,
        include: Optional[types.problemssettagsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique problemssettags record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            problemssettags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The found problemssettags record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemssettags = problemssettags.prisma().find_unique_or_raise(
            where={
                # problemssettags where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
        include: Optional[types.problemssettagsInclude] = None,
        order: Optional[Union[types.problemssettagsOrderByInput, List[types.problemssettagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemssettagsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple problemssettags records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of problemssettags records returned
        skip
            Ignore the first N results
        where
            problemssettags filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemssettags model
        order
            Order the returned problemssettags records by any field
        distinct
            Filter problemssettags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.problemssettags]
            The list of all problemssettags records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 problemssettags records
        problemssettags = problemssettags.prisma().find_many(take=10)

        # find the first 5 problemssettags records ordered by the problemsSetId field
        problemssettags = problemssettags.prisma().find_many(
            take=5,
            order={
                'problemsSetId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
        include: Optional[types.problemssettagsInclude] = None,
        order: Optional[Union[types.problemssettagsOrderByInput, List[types.problemssettagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemssettagsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single problemssettags record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemssettags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemssettags model
        order
            Order the returned problemssettags records by any field
        distinct
            Filter problemssettags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemssettags
            The first problemssettags record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemssettags record ordered by the tagid field
        problemssettags = problemssettags.prisma().find_first(
            skip=1,
            order={
                'tagid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
        include: Optional[types.problemssettagsInclude] = None,
        order: Optional[Union[types.problemssettagsOrderByInput, List[types.problemssettagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemssettagsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single problemssettags record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemssettags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemssettags model
        order
            Order the returned problemssettags records by any field
        distinct
            Filter problemssettags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemssettags
            The first problemssettags record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemssettags record ordered by the problemsSetId field
        problemssettags = problemssettags.prisma().find_first_or_raise(
            skip=1,
            order={
                'problemsSetId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.problemssettagsUpdateInput,
        where: types.problemssettagsWhereUniqueInput,
        include: Optional[types.problemssettagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single problemssettags record.

        Parameters
        ----------
        data
            problemssettags record data specifying what to update
        where
            problemssettags filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The updated problemssettags record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        problemssettags = problemssettags.prisma().update(
            where={
                # problemssettags where unique filter

            },
            data={
                # data to update the problemssettags record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.problemssettagsWhereUniqueInput,
        data: types.problemssettagsUpsertInput,
        include: Optional[types.problemssettagsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            problemssettags filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned problemssettags model

        Returns
        -------
        prisma.models.problemssettags
            The created or updated problemssettags record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemssettags = problemssettags.prisma().upsert(
            where={
                # problemssettags where unique filter
            },
            data={
                'create': {
                    # problemssettags data to be set if the record does not exist
                },
                'update': {
                    # problemssettags data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.problemssettagsUpdateManyMutationInput,
        where: types.problemssettagsWhereInput,
    ) -> int:
        """Update multiple problemssettags records

        Parameters
        ----------
        data
            problemssettags data to update the selected problemssettags records to
        where
            Filter to select the problemssettags records to update

        Returns
        -------
        int
            The total number of problemssettags records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all problemssettags records
        total = problemssettags.prisma().update_many(
            data={
                'tagid': 1621937922
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of problemssettags records present in the database

        Parameters
        ----------
        select
            Select the problemssettags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemssettags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemssettagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemssettags.prisma().count()

        # results: prisma.types.problemssettagsCountAggregateOutput
        results = problemssettags.prisma().count(
            select={
                '_all': True,
                'problemsSetId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.problemssettagsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
    ) -> types.problemssettagsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.problemssettagsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemssettagsWhereInput] = None,
        cursor: Optional[types.problemssettagsWhereUniqueInput] = None,
    ) -> Union[int, types.problemssettagsCountAggregateOutput]:
        """Count the number of problemssettags records present in the database

        Parameters
        ----------
        select
            Select the problemssettags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemssettags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemssettagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemssettags.prisma().count()

        # results: prisma.types.problemssettagsCountAggregateOutput
        results = problemssettags.prisma().count(
            select={
                '_all': True,
                'tagid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.problemssettagsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.problemssettagsWhereInput] = None
    ) -> int:
        """Delete multiple problemssettags records.

        Parameters
        ----------
        where
            Optional problemssettags filter to find the records to be deleted

        Returns
        -------
        int
            The total number of problemssettags records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all problemssettags records
        total = problemssettags.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.problemssettagsScalarFieldKeys'],
        *,
        where: Optional['types.problemssettagsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.problemssettagsAvgAggregateInput'] = None,
        sum: Optional['types.problemssettagsSumAggregateInput'] = None,
        min: Optional['types.problemssettagsMinAggregateInput'] = None,
        max: Optional['types.problemssettagsMaxAggregateInput'] = None,
        having: Optional['types.problemssettagsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.problemssettagsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.problemssettagsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.problemssettagsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.problemssettagsGroupByOutput']:
        """Group problemssettags records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar problemssettags fields to group records by
        where
            problemssettags filter to select records
        take
            Limit the maximum number of problemssettags records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.problemssettagsGroupByOutput]
            A list of dictionaries representing the problemssettags record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group problemssettags records by problemsSetId values
        # and count how many records are in each group
        results = problemssettags.prisma().group_by(
            ['problemsSetId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class problemtagsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.problemtags]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = problemtags.prisma().query_raw(
            'SELECT * FROM problemtags WHERE problemid = $1',
            1848832019,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.problemtags
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = problemtags.prisma().query_first(
            'SELECT * FROM problemtags WHERE tagid = $1',
            1921528400,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.problemtagsCreateInput,
        include: Optional[types.problemtagsInclude] = None
    ) -> _PrismaModelT:
        """Create a new problemtags record.

        Parameters
        ----------
        data
            problemtags record data
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The created problemtags record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a problemtags record from just the required fields
        problemtags = problemtags.prisma().create(
            data={
                # data to create a problemtags record
                'problemid': 2100427849,
                'tagid': 849140046,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.problemtagsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple problemtags records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of problemtags record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = problemtags.prisma().create_many(
            data=[
                {
                    # data to create a problemtags record
                    'problemid': 928152175,
                    'tagid': 273032060,
                },
                {
                    # data to create a problemtags record
                    'problemid': 982848517,
                    'tagid': 510737498,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.problemtagsWhereUniqueInput,
        include: Optional[types.problemtagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single problemtags record.

        Parameters
        ----------
        where
            problemtags filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The deleted problemtags record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemtags = problemtags.prisma().delete(
            where={
                # problemtags where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.problemtagsWhereUniqueInput,
        include: Optional[types.problemtagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique problemtags record.

        Parameters
        ----------
        where
            problemtags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The found problemtags record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemtags = problemtags.prisma().find_unique(
            where={
                # problemtags where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.problemtagsWhereUniqueInput,
        include: Optional[types.problemtagsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique problemtags record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            problemtags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The found problemtags record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemtags = problemtags.prisma().find_unique_or_raise(
            where={
                # problemtags where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
        include: Optional[types.problemtagsInclude] = None,
        order: Optional[Union[types.problemtagsOrderByInput, List[types.problemtagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemtagsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple problemtags records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of problemtags records returned
        skip
            Ignore the first N results
        where
            problemtags filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemtags model
        order
            Order the returned problemtags records by any field
        distinct
            Filter problemtags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.problemtags]
            The list of all problemtags records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 problemtags records
        problemtags = problemtags.prisma().find_many(take=10)

        # find the first 5 problemtags records ordered by the problemid field
        problemtags = problemtags.prisma().find_many(
            take=5,
            order={
                'problemid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
        include: Optional[types.problemtagsInclude] = None,
        order: Optional[Union[types.problemtagsOrderByInput, List[types.problemtagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemtagsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single problemtags record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemtags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemtags model
        order
            Order the returned problemtags records by any field
        distinct
            Filter problemtags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemtags
            The first problemtags record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemtags record ordered by the tagid field
        problemtags = problemtags.prisma().find_first(
            skip=1,
            order={
                'tagid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
        include: Optional[types.problemtagsInclude] = None,
        order: Optional[Union[types.problemtagsOrderByInput, List[types.problemtagsOrderByInput]]] = None,
        distinct: Optional[List[types.problemtagsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single problemtags record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            problemtags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned problemtags model
        order
            Order the returned problemtags records by any field
        distinct
            Filter problemtags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.problemtags
            The first problemtags record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second problemtags record ordered by the problemid field
        problemtags = problemtags.prisma().find_first_or_raise(
            skip=1,
            order={
                'problemid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.problemtagsUpdateInput,
        where: types.problemtagsWhereUniqueInput,
        include: Optional[types.problemtagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single problemtags record.

        Parameters
        ----------
        data
            problemtags record data specifying what to update
        where
            problemtags filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The updated problemtags record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        problemtags = problemtags.prisma().update(
            where={
                # problemtags where unique filter

            },
            data={
                # data to update the problemtags record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.problemtagsWhereUniqueInput,
        data: types.problemtagsUpsertInput,
        include: Optional[types.problemtagsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            problemtags filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned problemtags model

        Returns
        -------
        prisma.models.problemtags
            The created or updated problemtags record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        problemtags = problemtags.prisma().upsert(
            where={
                # problemtags where unique filter
            },
            data={
                'create': {
                    # problemtags data to be set if the record does not exist
                },
                'update': {
                    # problemtags data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.problemtagsUpdateManyMutationInput,
        where: types.problemtagsWhereInput,
    ) -> int:
        """Update multiple problemtags records

        Parameters
        ----------
        data
            problemtags data to update the selected problemtags records to
        where
            Filter to select the problemtags records to update

        Returns
        -------
        int
            The total number of problemtags records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all problemtags records
        total = problemtags.prisma().update_many(
            data={
                'tagid': 2117488267
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of problemtags records present in the database

        Parameters
        ----------
        select
            Select the problemtags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemtags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemtagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemtags.prisma().count()

        # results: prisma.types.problemtagsCountAggregateOutput
        results = problemtags.prisma().count(
            select={
                '_all': True,
                'problemid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.problemtagsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
    ) -> types.problemtagsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.problemtagsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.problemtagsWhereInput] = None,
        cursor: Optional[types.problemtagsWhereUniqueInput] = None,
    ) -> Union[int, types.problemtagsCountAggregateOutput]:
        """Count the number of problemtags records present in the database

        Parameters
        ----------
        select
            Select the problemtags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            problemtags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.problemtagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = problemtags.prisma().count()

        # results: prisma.types.problemtagsCountAggregateOutput
        results = problemtags.prisma().count(
            select={
                '_all': True,
                'tagid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.problemtagsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.problemtagsWhereInput] = None
    ) -> int:
        """Delete multiple problemtags records.

        Parameters
        ----------
        where
            Optional problemtags filter to find the records to be deleted

        Returns
        -------
        int
            The total number of problemtags records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all problemtags records
        total = problemtags.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.problemtagsScalarFieldKeys'],
        *,
        where: Optional['types.problemtagsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.problemtagsAvgAggregateInput'] = None,
        sum: Optional['types.problemtagsSumAggregateInput'] = None,
        min: Optional['types.problemtagsMinAggregateInput'] = None,
        max: Optional['types.problemtagsMaxAggregateInput'] = None,
        having: Optional['types.problemtagsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.problemtagsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.problemtagsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.problemtagsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.problemtagsGroupByOutput']:
        """Group problemtags records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar problemtags fields to group records by
        where
            problemtags filter to select records
        take
            Limit the maximum number of problemtags records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.problemtagsGroupByOutput]
            A list of dictionaries representing the problemtags record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group problemtags records by problemid values
        # and count how many records are in each group
        results = problemtags.prisma().group_by(
            ['problemid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class sectionsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.sections]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = sections.prisma().query_raw(
            'SELECT * FROM sections WHERE sectionid = $1',
            1401944936,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.sections
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = sections.prisma().query_first(
            'SELECT * FROM sections WHERE examtypeid = $1',
            1297607553,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.sectionsCreateInput,
        include: Optional[types.sectionsInclude] = None
    ) -> _PrismaModelT:
        """Create a new sections record.

        Parameters
        ----------
        data
            sections record data
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The created sections record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a sections record from just the required fields
        sections = sections.prisma().create(
            data={
                # data to create a sections record
                'name': 'fbjeiiffa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.sectionsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple sections records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of sections record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = sections.prisma().create_many(
            data=[
                {
                    # data to create a sections record
                    'name': 'jhgidcgbf',
                },
                {
                    # data to create a sections record
                    'name': 'bgjgecfejc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.sectionsWhereUniqueInput,
        include: Optional[types.sectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single sections record.

        Parameters
        ----------
        where
            sections filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The deleted sections record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sections = sections.prisma().delete(
            where={
                'sectionid': 169262781,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.sectionsWhereUniqueInput,
        include: Optional[types.sectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique sections record.

        Parameters
        ----------
        where
            sections filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The found sections record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sections = sections.prisma().find_unique(
            where={
                'sectionid': 1023081650,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.sectionsWhereUniqueInput,
        include: Optional[types.sectionsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique sections record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            sections filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The found sections record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sections = sections.prisma().find_unique_or_raise(
            where={
                'sectionid': 327681027,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
        include: Optional[types.sectionsInclude] = None,
        order: Optional[Union[types.sectionsOrderByInput, List[types.sectionsOrderByInput]]] = None,
        distinct: Optional[List[types.sectionsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple sections records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of sections records returned
        skip
            Ignore the first N results
        where
            sections filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sections model
        order
            Order the returned sections records by any field
        distinct
            Filter sections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.sections]
            The list of all sections records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 sections records
        sections = sections.prisma().find_many(take=10)

        # find the first 5 sections records ordered by the name field
        sections = sections.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
        include: Optional[types.sectionsInclude] = None,
        order: Optional[Union[types.sectionsOrderByInput, List[types.sectionsOrderByInput]]] = None,
        distinct: Optional[List[types.sectionsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single sections record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            sections filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sections model
        order
            Order the returned sections records by any field
        distinct
            Filter sections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.sections
            The first sections record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second sections record ordered by the description field
        sections = sections.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
        include: Optional[types.sectionsInclude] = None,
        order: Optional[Union[types.sectionsOrderByInput, List[types.sectionsOrderByInput]]] = None,
        distinct: Optional[List[types.sectionsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single sections record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            sections filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sections model
        order
            Order the returned sections records by any field
        distinct
            Filter sections records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.sections
            The first sections record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second sections record ordered by the sectionid field
        sections = sections.prisma().find_first_or_raise(
            skip=1,
            order={
                'sectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.sectionsUpdateInput,
        where: types.sectionsWhereUniqueInput,
        include: Optional[types.sectionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single sections record.

        Parameters
        ----------
        data
            sections record data specifying what to update
        where
            sections filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The updated sections record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        sections = sections.prisma().update(
            where={
                'sectionid': 527748992,
            },
            data={
                # data to update the sections record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.sectionsWhereUniqueInput,
        data: types.sectionsUpsertInput,
        include: Optional[types.sectionsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            sections filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned sections model

        Returns
        -------
        prisma.models.sections
            The created or updated sections record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sections = sections.prisma().upsert(
            where={
                'sectionid': 2029357497,
            },
            data={
                'create': {
                    'sectionid': 2029357497,
                    'name': 'bgjgecfejc',
                },
                'update': {
                    'name': 'bgjgecfejc',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.sectionsUpdateManyMutationInput,
        where: types.sectionsWhereInput,
    ) -> int:
        """Update multiple sections records

        Parameters
        ----------
        data
            sections data to update the selected sections records to
        where
            Filter to select the sections records to update

        Returns
        -------
        int
            The total number of sections records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all sections records
        total = sections.prisma().update_many(
            data={
                'examtypeid': 1318597118
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of sections records present in the database

        Parameters
        ----------
        select
            Select the sections fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            sections filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.sectionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = sections.prisma().count()

        # results: prisma.types.sectionsCountAggregateOutput
        results = sections.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.sectionsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
    ) -> types.sectionsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.sectionsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sectionsWhereInput] = None,
        cursor: Optional[types.sectionsWhereUniqueInput] = None,
    ) -> Union[int, types.sectionsCountAggregateOutput]:
        """Count the number of sections records present in the database

        Parameters
        ----------
        select
            Select the sections fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            sections filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.sectionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = sections.prisma().count()

        # results: prisma.types.sectionsCountAggregateOutput
        results = sections.prisma().count(
            select={
                '_all': True,
                'description': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.sectionsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.sectionsWhereInput] = None
    ) -> int:
        """Delete multiple sections records.

        Parameters
        ----------
        where
            Optional sections filter to find the records to be deleted

        Returns
        -------
        int
            The total number of sections records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all sections records
        total = sections.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.sectionsScalarFieldKeys'],
        *,
        where: Optional['types.sectionsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.sectionsAvgAggregateInput'] = None,
        sum: Optional['types.sectionsSumAggregateInput'] = None,
        min: Optional['types.sectionsMinAggregateInput'] = None,
        max: Optional['types.sectionsMaxAggregateInput'] = None,
        having: Optional['types.sectionsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.sectionsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.sectionsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.sectionsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.sectionsGroupByOutput']:
        """Group sections records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar sections fields to group records by
        where
            sections filter to select records
        take
            Limit the maximum number of sections records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.sectionsGroupByOutput]
            A list of dictionaries representing the sections record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group sections records by sectionid values
        # and count how many records are in each group
        results = sections.prisma().group_by(
            ['sectionid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class tagsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.tags]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = tags.prisma().query_raw(
            'SELECT * FROM tags WHERE tagid = $1',
            2122112351,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.tags
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = tags.prisma().query_first(
            'SELECT * FROM tags WHERE name = $1',
            'bacejedaca',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.tagsCreateInput,
        include: Optional[types.tagsInclude] = None
    ) -> _PrismaModelT:
        """Create a new tags record.

        Parameters
        ----------
        data
            tags record data
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The created tags record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a tags record from just the required fields
        tags = tags.prisma().create(
            data={
                # data to create a tags record
                'name': 'bhbhdahfaj',
                'examtypeid': 1598124042,
                'sectionid': 817623163,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.tagsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple tags records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of tags record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = tags.prisma().create_many(
            data=[
                {
                    # data to create a tags record
                    'name': 'badaffhddg',
                    'examtypeid': 1131525873,
                    'sectionid': 210666198,
                },
                {
                    # data to create a tags record
                    'name': 'bchgafhjed',
                    'examtypeid': 745569348,
                    'sectionid': 307876141,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.tagsWhereUniqueInput,
        include: Optional[types.tagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single tags record.

        Parameters
        ----------
        where
            tags filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The deleted tags record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tags = tags.prisma().delete(
            where={
                'tagid': 1674049122,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.tagsWhereUniqueInput,
        include: Optional[types.tagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique tags record.

        Parameters
        ----------
        where
            tags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The found tags record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tags = tags.prisma().find_unique(
            where={
                'tagid': 1526854643,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.tagsWhereUniqueInput,
        include: Optional[types.tagsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique tags record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            tags filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The found tags record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tags = tags.prisma().find_unique_or_raise(
            where={
                'tagid': 958077104,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
        include: Optional[types.tagsInclude] = None,
        order: Optional[Union[types.tagsOrderByInput, List[types.tagsOrderByInput]]] = None,
        distinct: Optional[List[types.tagsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple tags records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of tags records returned
        skip
            Ignore the first N results
        where
            tags filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned tags model
        order
            Order the returned tags records by any field
        distinct
            Filter tags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.tags]
            The list of all tags records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 tags records
        tags = tags.prisma().find_many(take=10)

        # find the first 5 tags records ordered by the examtypeid field
        tags = tags.prisma().find_many(
            take=5,
            order={
                'examtypeid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
        include: Optional[types.tagsInclude] = None,
        order: Optional[Union[types.tagsOrderByInput, List[types.tagsOrderByInput]]] = None,
        distinct: Optional[List[types.tagsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single tags record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            tags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned tags model
        order
            Order the returned tags records by any field
        distinct
            Filter tags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.tags
            The first tags record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second tags record ordered by the sectionid field
        tags = tags.prisma().find_first(
            skip=1,
            order={
                'sectionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
        include: Optional[types.tagsInclude] = None,
        order: Optional[Union[types.tagsOrderByInput, List[types.tagsOrderByInput]]] = None,
        distinct: Optional[List[types.tagsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single tags record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            tags filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned tags model
        order
            Order the returned tags records by any field
        distinct
            Filter tags records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.tags
            The first tags record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second tags record ordered by the tagid field
        tags = tags.prisma().find_first_or_raise(
            skip=1,
            order={
                'tagid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.tagsUpdateInput,
        where: types.tagsWhereUniqueInput,
        include: Optional[types.tagsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single tags record.

        Parameters
        ----------
        data
            tags record data specifying what to update
        where
            tags filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The updated tags record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        tags = tags.prisma().update(
            where={
                'tagid': 1513050921,
            },
            data={
                # data to update the tags record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.tagsWhereUniqueInput,
        data: types.tagsUpsertInput,
        include: Optional[types.tagsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            tags filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned tags model

        Returns
        -------
        prisma.models.tags
            The created or updated tags record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tags = tags.prisma().upsert(
            where={
                'tagid': 204674734,
            },
            data={
                'create': {
                    'tagid': 204674734,
                    'name': 'bchgafhjed',
                    'examtypeid': 745569348,
                    'sectionid': 307876141,
                },
                'update': {
                    'name': 'bchgafhjed',
                    'examtypeid': 745569348,
                    'sectionid': 307876141,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.tagsUpdateManyMutationInput,
        where: types.tagsWhereInput,
    ) -> int:
        """Update multiple tags records

        Parameters
        ----------
        data
            tags data to update the selected tags records to
        where
            Filter to select the tags records to update

        Returns
        -------
        int
            The total number of tags records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all tags records
        total = tags.prisma().update_many(
            data={
                'name': 'caghgfbggd'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of tags records present in the database

        Parameters
        ----------
        select
            Select the tags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            tags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.tagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = tags.prisma().count()

        # results: prisma.types.tagsCountAggregateOutput
        results = tags.prisma().count(
            select={
                '_all': True,
                'examtypeid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.tagsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
    ) -> types.tagsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.tagsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.tagsWhereInput] = None,
        cursor: Optional[types.tagsWhereUniqueInput] = None,
    ) -> Union[int, types.tagsCountAggregateOutput]:
        """Count the number of tags records present in the database

        Parameters
        ----------
        select
            Select the tags fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            tags filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.tagsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = tags.prisma().count()

        # results: prisma.types.tagsCountAggregateOutput
        results = tags.prisma().count(
            select={
                '_all': True,
                'sectionid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.tagsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.tagsWhereInput] = None
    ) -> int:
        """Delete multiple tags records.

        Parameters
        ----------
        where
            Optional tags filter to find the records to be deleted

        Returns
        -------
        int
            The total number of tags records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all tags records
        total = tags.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.tagsScalarFieldKeys'],
        *,
        where: Optional['types.tagsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.tagsAvgAggregateInput'] = None,
        sum: Optional['types.tagsSumAggregateInput'] = None,
        min: Optional['types.tagsMinAggregateInput'] = None,
        max: Optional['types.tagsMaxAggregateInput'] = None,
        having: Optional['types.tagsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.tagsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.tagsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.tagsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.tagsGroupByOutput']:
        """Group tags records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar tags fields to group records by
        where
            tags filter to select records
        take
            Limit the maximum number of tags records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.tagsGroupByOutput]
            A list of dictionaries representing the tags record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group tags records by tagid values
        # and count how many records are in each group
        results = tags.prisma().group_by(
            ['tagid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class userattempt_selectedoptionsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.userattempt_selectedoptions]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = userattempt_selectedoptions.prisma().query_raw(
            'SELECT * FROM userattempt_selectedoptions WHERE id = $1',
            1183911900,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = userattempt_selectedoptions.prisma().query_first(
            'SELECT * FROM userattempt_selectedoptions WHERE userattemptid = $1',
            1589704933,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.userattempt_selectedoptionsCreateInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None
    ) -> _PrismaModelT:
        """Create a new userattempt_selectedoptions record.

        Parameters
        ----------
        data
            userattempt_selectedoptions record data
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The created userattempt_selectedoptions record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a userattempt_selectedoptions record from just the required fields
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().create(
            data={
                # data to create a userattempt_selectedoptions record
                'userattemptid': 1243475898,
                'optionid': 1369828971,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.userattempt_selectedoptionsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple userattempt_selectedoptions records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of userattempt_selectedoptions record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = userattempt_selectedoptions.prisma().create_many(
            data=[
                {
                    # data to create a userattempt_selectedoptions record
                    'userattemptid': 1678593480,
                    'optionid': 403521121,
                },
                {
                    # data to create a userattempt_selectedoptions record
                    'userattemptid': 648760710,
                    'optionid': 607323719,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.userattempt_selectedoptionsWhereUniqueInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single userattempt_selectedoptions record.

        Parameters
        ----------
        where
            userattempt_selectedoptions filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The deleted userattempt_selectedoptions record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().delete(
            where={
                'id': 1468890740,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.userattempt_selectedoptionsWhereUniqueInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique userattempt_selectedoptions record.

        Parameters
        ----------
        where
            userattempt_selectedoptions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The found userattempt_selectedoptions record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_unique(
            where={
                'id': 629039005,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.userattempt_selectedoptionsWhereUniqueInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique userattempt_selectedoptions record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            userattempt_selectedoptions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The found userattempt_selectedoptions record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_unique_or_raise(
            where={
                'id': 1214168082,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
        include: Optional[types.userattempt_selectedoptionsInclude] = None,
        order: Optional[Union[types.userattempt_selectedoptionsOrderByInput, List[types.userattempt_selectedoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.userattempt_selectedoptionsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple userattempt_selectedoptions records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of userattempt_selectedoptions records returned
        skip
            Ignore the first N results
        where
            userattempt_selectedoptions filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model
        order
            Order the returned userattempt_selectedoptions records by any field
        distinct
            Filter userattempt_selectedoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.userattempt_selectedoptions]
            The list of all userattempt_selectedoptions records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 userattempt_selectedoptions records
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_many(take=10)

        # find the first 5 userattempt_selectedoptions records ordered by the optionid field
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_many(
            take=5,
            order={
                'optionid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
        include: Optional[types.userattempt_selectedoptionsInclude] = None,
        order: Optional[Union[types.userattempt_selectedoptionsOrderByInput, List[types.userattempt_selectedoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.userattempt_selectedoptionsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single userattempt_selectedoptions record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userattempt_selectedoptions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model
        order
            Order the returned userattempt_selectedoptions records by any field
        distinct
            Filter userattempt_selectedoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The first userattempt_selectedoptions record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userattempt_selectedoptions record ordered by the id field
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
        include: Optional[types.userattempt_selectedoptionsInclude] = None,
        order: Optional[Union[types.userattempt_selectedoptionsOrderByInput, List[types.userattempt_selectedoptionsOrderByInput]]] = None,
        distinct: Optional[List[types.userattempt_selectedoptionsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single userattempt_selectedoptions record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userattempt_selectedoptions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model
        order
            Order the returned userattempt_selectedoptions records by any field
        distinct
            Filter userattempt_selectedoptions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The first userattempt_selectedoptions record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userattempt_selectedoptions record ordered by the userattemptid field
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().find_first_or_raise(
            skip=1,
            order={
                'userattemptid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.userattempt_selectedoptionsUpdateInput,
        where: types.userattempt_selectedoptionsWhereUniqueInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single userattempt_selectedoptions record.

        Parameters
        ----------
        data
            userattempt_selectedoptions record data specifying what to update
        where
            userattempt_selectedoptions filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The updated userattempt_selectedoptions record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().update(
            where={
                'id': 898613219,
            },
            data={
                # data to update the userattempt_selectedoptions record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.userattempt_selectedoptionsWhereUniqueInput,
        data: types.userattempt_selectedoptionsUpsertInput,
        include: Optional[types.userattempt_selectedoptionsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            userattempt_selectedoptions filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned userattempt_selectedoptions model

        Returns
        -------
        prisma.models.userattempt_selectedoptions
            The created or updated userattempt_selectedoptions record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempt_selectedoptions = userattempt_selectedoptions.prisma().upsert(
            where={
                'id': 658378208,
            },
            data={
                'create': {
                    'id': 658378208,
                    'userattemptid': 648760710,
                    'optionid': 607323719,
                },
                'update': {
                    'userattemptid': 648760710,
                    'optionid': 607323719,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.userattempt_selectedoptionsUpdateManyMutationInput,
        where: types.userattempt_selectedoptionsWhereInput,
    ) -> int:
        """Update multiple userattempt_selectedoptions records

        Parameters
        ----------
        data
            userattempt_selectedoptions data to update the selected userattempt_selectedoptions records to
        where
            Filter to select the userattempt_selectedoptions records to update

        Returns
        -------
        int
            The total number of userattempt_selectedoptions records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all userattempt_selectedoptions records
        total = userattempt_selectedoptions.prisma().update_many(
            data={
                'optionid': 954620057
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of userattempt_selectedoptions records present in the database

        Parameters
        ----------
        select
            Select the userattempt_selectedoptions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userattempt_selectedoptions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userattempt_selectedoptionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userattempt_selectedoptions.prisma().count()

        # results: prisma.types.userattempt_selectedoptionsCountAggregateOutput
        results = userattempt_selectedoptions.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.userattempt_selectedoptionsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
    ) -> types.userattempt_selectedoptionsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.userattempt_selectedoptionsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None,
        cursor: Optional[types.userattempt_selectedoptionsWhereUniqueInput] = None,
    ) -> Union[int, types.userattempt_selectedoptionsCountAggregateOutput]:
        """Count the number of userattempt_selectedoptions records present in the database

        Parameters
        ----------
        select
            Select the userattempt_selectedoptions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userattempt_selectedoptions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userattempt_selectedoptionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userattempt_selectedoptions.prisma().count()

        # results: prisma.types.userattempt_selectedoptionsCountAggregateOutput
        results = userattempt_selectedoptions.prisma().count(
            select={
                '_all': True,
                'userattemptid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.userattempt_selectedoptionsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.userattempt_selectedoptionsWhereInput] = None
    ) -> int:
        """Delete multiple userattempt_selectedoptions records.

        Parameters
        ----------
        where
            Optional userattempt_selectedoptions filter to find the records to be deleted

        Returns
        -------
        int
            The total number of userattempt_selectedoptions records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all userattempt_selectedoptions records
        total = userattempt_selectedoptions.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.userattempt_selectedoptionsScalarFieldKeys'],
        *,
        where: Optional['types.userattempt_selectedoptionsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.userattempt_selectedoptionsAvgAggregateInput'] = None,
        sum: Optional['types.userattempt_selectedoptionsSumAggregateInput'] = None,
        min: Optional['types.userattempt_selectedoptionsMinAggregateInput'] = None,
        max: Optional['types.userattempt_selectedoptionsMaxAggregateInput'] = None,
        having: Optional['types.userattempt_selectedoptionsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.userattempt_selectedoptionsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.userattempt_selectedoptionsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.userattempt_selectedoptionsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.userattempt_selectedoptionsGroupByOutput']:
        """Group userattempt_selectedoptions records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar userattempt_selectedoptions fields to group records by
        where
            userattempt_selectedoptions filter to select records
        take
            Limit the maximum number of userattempt_selectedoptions records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.userattempt_selectedoptionsGroupByOutput]
            A list of dictionaries representing the userattempt_selectedoptions record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group userattempt_selectedoptions records by optionid values
        # and count how many records are in each group
        results = userattempt_selectedoptions.prisma().group_by(
            ['optionid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class userattemptsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.userattempts]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = userattempts.prisma().query_raw(
            'SELECT * FROM userattempts WHERE attemptid = $1',
            1214809950,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.userattempts
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = userattempts.prisma().query_first(
            'SELECT * FROM userattempts WHERE userid = $1',
            1047820095,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.userattemptsCreateInput,
        include: Optional[types.userattemptsInclude] = None
    ) -> _PrismaModelT:
        """Create a new userattempts record.

        Parameters
        ----------
        data
            userattempts record data
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The created userattempts record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a userattempts record from just the required fields
        userattempts = userattempts.prisma().create(
            data={
                # data to create a userattempts record
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.userattemptsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple userattempts records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of userattempts record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = userattempts.prisma().create_many(
            data=[
                {
                    # data to create a userattempts record
                },
                {
                    # data to create a userattempts record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.userattemptsWhereUniqueInput,
        include: Optional[types.userattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single userattempts record.

        Parameters
        ----------
        where
            userattempts filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The deleted userattempts record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempts = userattempts.prisma().delete(
            where={
                'attemptid': 1302734860,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.userattemptsWhereUniqueInput,
        include: Optional[types.userattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique userattempts record.

        Parameters
        ----------
        where
            userattempts filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The found userattempts record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempts = userattempts.prisma().find_unique(
            where={
                'attemptid': 893052245,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.userattemptsWhereUniqueInput,
        include: Optional[types.userattemptsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique userattempts record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            userattempts filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The found userattempts record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempts = userattempts.prisma().find_unique_or_raise(
            where={
                'attemptid': 280083306,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
        include: Optional[types.userattemptsInclude] = None,
        order: Optional[Union[types.userattemptsOrderByInput, List[types.userattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.userattemptsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple userattempts records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of userattempts records returned
        skip
            Ignore the first N results
        where
            userattempts filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempts model
        order
            Order the returned userattempts records by any field
        distinct
            Filter userattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.userattempts]
            The list of all userattempts records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 userattempts records
        userattempts = userattempts.prisma().find_many(take=10)

        # find the first 5 userattempts records ordered by the problemid field
        userattempts = userattempts.prisma().find_many(
            take=5,
            order={
                'problemid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
        include: Optional[types.userattemptsInclude] = None,
        order: Optional[Union[types.userattemptsOrderByInput, List[types.userattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.userattemptsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single userattempts record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userattempts filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempts model
        order
            Order the returned userattempts records by any field
        distinct
            Filter userattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userattempts
            The first userattempts record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userattempts record ordered by the timetaken field
        userattempts = userattempts.prisma().find_first(
            skip=1,
            order={
                'timetaken': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
        include: Optional[types.userattemptsInclude] = None,
        order: Optional[Union[types.userattemptsOrderByInput, List[types.userattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.userattemptsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single userattempts record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userattempts filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userattempts model
        order
            Order the returned userattempts records by any field
        distinct
            Filter userattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userattempts
            The first userattempts record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userattempts record ordered by the attemptdate field
        userattempts = userattempts.prisma().find_first_or_raise(
            skip=1,
            order={
                'attemptdate': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.userattemptsUpdateInput,
        where: types.userattemptsWhereUniqueInput,
        include: Optional[types.userattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single userattempts record.

        Parameters
        ----------
        data
            userattempts record data specifying what to update
        where
            userattempts filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The updated userattempts record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userattempts = userattempts.prisma().update(
            where={
                'attemptid': 549668955,
            },
            data={
                # data to update the userattempts record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.userattemptsWhereUniqueInput,
        data: types.userattemptsUpsertInput,
        include: Optional[types.userattemptsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            userattempts filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned userattempts model

        Returns
        -------
        prisma.models.userattempts
            The created or updated userattempts record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userattempts = userattempts.prisma().upsert(
            where={
                'attemptid': 76790008,
            },
            data={
                'create': {
                    'attemptid': 76790008,
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.userattemptsUpdateManyMutationInput,
        where: types.userattemptsWhereInput,
    ) -> int:
        """Update multiple userattempts records

        Parameters
        ----------
        data
            userattempts data to update the selected userattempts records to
        where
            Filter to select the userattempts records to update

        Returns
        -------
        int
            The total number of userattempts records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all userattempts records
        total = userattempts.prisma().update_many(
            data={
                'partialcorrectnessscore': Decimal('2098299345.24590')
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of userattempts records present in the database

        Parameters
        ----------
        select
            Select the userattempts fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userattempts filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userattemptsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userattempts.prisma().count()

        # results: prisma.types.userattemptsCountAggregateOutput
        results = userattempts.prisma().count(
            select={
                '_all': True,
                'iscorrect': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.userattemptsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
    ) -> types.userattemptsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.userattemptsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userattemptsWhereInput] = None,
        cursor: Optional[types.userattemptsWhereUniqueInput] = None,
    ) -> Union[int, types.userattemptsCountAggregateOutput]:
        """Count the number of userattempts records present in the database

        Parameters
        ----------
        select
            Select the userattempts fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userattempts filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userattemptsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userattempts.prisma().count()

        # results: prisma.types.userattemptsCountAggregateOutput
        results = userattempts.prisma().count(
            select={
                '_all': True,
                'attemptid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.userattemptsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.userattemptsWhereInput] = None
    ) -> int:
        """Delete multiple userattempts records.

        Parameters
        ----------
        where
            Optional userattempts filter to find the records to be deleted

        Returns
        -------
        int
            The total number of userattempts records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all userattempts records
        total = userattempts.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.userattemptsScalarFieldKeys'],
        *,
        where: Optional['types.userattemptsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.userattemptsAvgAggregateInput'] = None,
        sum: Optional['types.userattemptsSumAggregateInput'] = None,
        min: Optional['types.userattemptsMinAggregateInput'] = None,
        max: Optional['types.userattemptsMaxAggregateInput'] = None,
        having: Optional['types.userattemptsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.userattemptsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.userattemptsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.userattemptsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.userattemptsGroupByOutput']:
        """Group userattempts records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar userattempts fields to group records by
        where
            userattempts filter to select records
        take
            Limit the maximum number of userattempts records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.userattemptsGroupByOutput]
            A list of dictionaries representing the userattempts record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group userattempts records by userid values
        # and count how many records are in each group
        results = userattempts.prisma().group_by(
            ['userid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class usermocktestattemptsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.usermocktestattempts]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = usermocktestattempts.prisma().query_raw(
            'SELECT * FROM usermocktestattempts WHERE attemptid = $1',
            811863863,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.usermocktestattempts
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = usermocktestattempts.prisma().query_first(
            'SELECT * FROM usermocktestattempts WHERE userid = $1',
            1388801188,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.usermocktestattemptsCreateInput,
        include: Optional[types.usermocktestattemptsInclude] = None
    ) -> _PrismaModelT:
        """Create a new usermocktestattempts record.

        Parameters
        ----------
        data
            usermocktestattempts record data
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The created usermocktestattempts record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a usermocktestattempts record from just the required fields
        usermocktestattempts = usermocktestattempts.prisma().create(
            data={
                # data to create a usermocktestattempts record
                'starttime': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.usermocktestattemptsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple usermocktestattempts records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of usermocktestattempts record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = usermocktestattempts.prisma().create_many(
            data=[
                {
                    # data to create a usermocktestattempts record
                    'starttime': datetime.datetime.utcnow(),
                },
                {
                    # data to create a usermocktestattempts record
                    'starttime': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.usermocktestattemptsWhereUniqueInput,
        include: Optional[types.usermocktestattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single usermocktestattempts record.

        Parameters
        ----------
        where
            usermocktestattempts filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The deleted usermocktestattempts record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermocktestattempts = usermocktestattempts.prisma().delete(
            where={
                'attemptid': 752577037,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.usermocktestattemptsWhereUniqueInput,
        include: Optional[types.usermocktestattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique usermocktestattempts record.

        Parameters
        ----------
        where
            usermocktestattempts filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The found usermocktestattempts record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermocktestattempts = usermocktestattempts.prisma().find_unique(
            where={
                'attemptid': 1187663298,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.usermocktestattemptsWhereUniqueInput,
        include: Optional[types.usermocktestattemptsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique usermocktestattempts record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            usermocktestattempts filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The found usermocktestattempts record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermocktestattempts = usermocktestattempts.prisma().find_unique_or_raise(
            where={
                'attemptid': 769681363,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
        include: Optional[types.usermocktestattemptsInclude] = None,
        order: Optional[Union[types.usermocktestattemptsOrderByInput, List[types.usermocktestattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.usermocktestattemptsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple usermocktestattempts records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of usermocktestattempts records returned
        skip
            Ignore the first N results
        where
            usermocktestattempts filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model
        order
            Order the returned usermocktestattempts records by any field
        distinct
            Filter usermocktestattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.usermocktestattempts]
            The list of all usermocktestattempts records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 usermocktestattempts records
        usermocktestattempts = usermocktestattempts.prisma().find_many(take=10)

        # find the first 5 usermocktestattempts records ordered by the mocktestid field
        usermocktestattempts = usermocktestattempts.prisma().find_many(
            take=5,
            order={
                'mocktestid': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
        include: Optional[types.usermocktestattemptsInclude] = None,
        order: Optional[Union[types.usermocktestattemptsOrderByInput, List[types.usermocktestattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.usermocktestattemptsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single usermocktestattempts record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            usermocktestattempts filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model
        order
            Order the returned usermocktestattempts records by any field
        distinct
            Filter usermocktestattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.usermocktestattempts
            The first usermocktestattempts record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second usermocktestattempts record ordered by the starttime field
        usermocktestattempts = usermocktestattempts.prisma().find_first(
            skip=1,
            order={
                'starttime': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
        include: Optional[types.usermocktestattemptsInclude] = None,
        order: Optional[Union[types.usermocktestattemptsOrderByInput, List[types.usermocktestattemptsOrderByInput]]] = None,
        distinct: Optional[List[types.usermocktestattemptsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single usermocktestattempts record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            usermocktestattempts filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model
        order
            Order the returned usermocktestattempts records by any field
        distinct
            Filter usermocktestattempts records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.usermocktestattempts
            The first usermocktestattempts record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second usermocktestattempts record ordered by the endtime field
        usermocktestattempts = usermocktestattempts.prisma().find_first_or_raise(
            skip=1,
            order={
                'endtime': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.usermocktestattemptsUpdateInput,
        where: types.usermocktestattemptsWhereUniqueInput,
        include: Optional[types.usermocktestattemptsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single usermocktestattempts record.

        Parameters
        ----------
        data
            usermocktestattempts record data specifying what to update
        where
            usermocktestattempts filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The updated usermocktestattempts record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        usermocktestattempts = usermocktestattempts.prisma().update(
            where={
                'attemptid': 1214295824,
            },
            data={
                # data to update the usermocktestattempts record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.usermocktestattemptsWhereUniqueInput,
        data: types.usermocktestattemptsUpsertInput,
        include: Optional[types.usermocktestattemptsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            usermocktestattempts filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned usermocktestattempts model

        Returns
        -------
        prisma.models.usermocktestattempts
            The created or updated usermocktestattempts record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermocktestattempts = usermocktestattempts.prisma().upsert(
            where={
                'attemptid': 1021417993,
            },
            data={
                'create': {
                    'attemptid': 1021417993,
                    'starttime': datetime.datetime.utcnow(),
                },
                'update': {
                    'starttime': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.usermocktestattemptsUpdateManyMutationInput,
        where: types.usermocktestattemptsWhereInput,
    ) -> int:
        """Update multiple usermocktestattempts records

        Parameters
        ----------
        data
            usermocktestattempts data to update the selected usermocktestattempts records to
        where
            Filter to select the usermocktestattempts records to update

        Returns
        -------
        int
            The total number of usermocktestattempts records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all usermocktestattempts records
        total = usermocktestattempts.prisma().update_many(
            data={
                'totalscore': Decimal('351503374.132118')
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of usermocktestattempts records present in the database

        Parameters
        ----------
        select
            Select the usermocktestattempts fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            usermocktestattempts filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usermocktestattemptsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = usermocktestattempts.prisma().count()

        # results: prisma.types.usermocktestattemptsCountAggregateOutput
        results = usermocktestattempts.prisma().count(
            select={
                '_all': True,
                'isofficialattempt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.usermocktestattemptsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
    ) -> types.usermocktestattemptsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.usermocktestattemptsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usermocktestattemptsWhereInput] = None,
        cursor: Optional[types.usermocktestattemptsWhereUniqueInput] = None,
    ) -> Union[int, types.usermocktestattemptsCountAggregateOutput]:
        """Count the number of usermocktestattempts records present in the database

        Parameters
        ----------
        select
            Select the usermocktestattempts fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            usermocktestattempts filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usermocktestattemptsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = usermocktestattempts.prisma().count()

        # results: prisma.types.usermocktestattemptsCountAggregateOutput
        results = usermocktestattempts.prisma().count(
            select={
                '_all': True,
                'attemptid': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.usermocktestattemptsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.usermocktestattemptsWhereInput] = None
    ) -> int:
        """Delete multiple usermocktestattempts records.

        Parameters
        ----------
        where
            Optional usermocktestattempts filter to find the records to be deleted

        Returns
        -------
        int
            The total number of usermocktestattempts records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all usermocktestattempts records
        total = usermocktestattempts.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.usermocktestattemptsScalarFieldKeys'],
        *,
        where: Optional['types.usermocktestattemptsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.usermocktestattemptsAvgAggregateInput'] = None,
        sum: Optional['types.usermocktestattemptsSumAggregateInput'] = None,
        min: Optional['types.usermocktestattemptsMinAggregateInput'] = None,
        max: Optional['types.usermocktestattemptsMaxAggregateInput'] = None,
        having: Optional['types.usermocktestattemptsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.usermocktestattemptsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.usermocktestattemptsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.usermocktestattemptsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.usermocktestattemptsGroupByOutput']:
        """Group usermocktestattempts records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar usermocktestattempts fields to group records by
        where
            usermocktestattempts filter to select records
        take
            Limit the maximum number of usermocktestattempts records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.usermocktestattemptsGroupByOutput]
            A list of dictionaries representing the usermocktestattempts record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group usermocktestattempts records by userid values
        # and count how many records are in each group
        results = usermocktestattempts.prisma().group_by(
            ['userid'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class usersActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.users]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = users.prisma().query_raw(
            'SELECT * FROM users WHERE userid = $1',
            369732668,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.users
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = users.prisma().query_first(
            'SELECT * FROM users WHERE username = $1',
            'bbjbcdfabd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.usersCreateInput,
        include: Optional[types.usersInclude] = None
    ) -> _PrismaModelT:
        """Create a new users record.

        Parameters
        ----------
        data
            users record data
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The created users record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a users record from just the required fields
        users = users.prisma().create(
            data={
                # data to create a users record
                'username': 'gchfgbcec',
                'password': 'bihcjfcjah',
                'email': 'bhjdcicaii',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.usersCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple users records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of users record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = users.prisma().create_many(
            data=[
                {
                    # data to create a users record
                    'username': 'bibedjhcej',
                    'password': 'bjcdajabfa',
                    'email': 'bchhceeeff',
                },
                {
                    # data to create a users record
                    'username': 'bbgaifhdaa',
                    'password': 'dgbcdaegb',
                    'email': 'beagfbbjig',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.usersWhereUniqueInput,
        include: Optional[types.usersInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single users record.

        Parameters
        ----------
        where
            users filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The deleted users record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = users.prisma().delete(
            where={
                'userid': 1482877891,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.usersWhereUniqueInput,
        include: Optional[types.usersInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique users record.

        Parameters
        ----------
        where
            users filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The found users record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = users.prisma().find_unique(
            where={
                'userid': 566623296,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.usersWhereUniqueInput,
        include: Optional[types.usersInclude] = None
    ) -> _PrismaModelT:
        """Find a unique users record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            users filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The found users record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = users.prisma().find_unique_or_raise(
            where={
                'userid': 229119865,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
        include: Optional[types.usersInclude] = None,
        order: Optional[Union[types.usersOrderByInput, List[types.usersOrderByInput]]] = None,
        distinct: Optional[List[types.usersScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple users records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of users records returned
        skip
            Ignore the first N results
        where
            users filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.users]
            The list of all users records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 users records
        users = users.prisma().find_many(take=10)

        # find the first 5 users records ordered by the password field
        users = users.prisma().find_many(
            take=5,
            order={
                'password': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
        include: Optional[types.usersInclude] = None,
        order: Optional[Union[types.usersOrderByInput, List[types.usersOrderByInput]]] = None,
        distinct: Optional[List[types.usersScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single users record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            users filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.users
            The first users record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second users record ordered by the email field
        users = users.prisma().find_first(
            skip=1,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
        include: Optional[types.usersInclude] = None,
        order: Optional[Union[types.usersOrderByInput, List[types.usersOrderByInput]]] = None,
        distinct: Optional[List[types.usersScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single users record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            users filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.users
            The first users record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second users record ordered by the registrationdate field
        users = users.prisma().find_first_or_raise(
            skip=1,
            order={
                'registrationdate': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.usersUpdateInput,
        where: types.usersWhereUniqueInput,
        include: Optional[types.usersInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single users record.

        Parameters
        ----------
        data
            users record data specifying what to update
        where
            users filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The updated users record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = users.prisma().update(
            where={
                'userid': 1750011070,
            },
            data={
                # data to update the users record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.usersWhereUniqueInput,
        data: types.usersUpsertInput,
        include: Optional[types.usersInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            users filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The created or updated users record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = users.prisma().upsert(
            where={
                'userid': 410943775,
            },
            data={
                'create': {
                    'userid': 410943775,
                    'username': 'bbgaifhdaa',
                    'password': 'dgbcdaegb',
                    'email': 'beagfbbjig',
                },
                'update': {
                    'username': 'bbgaifhdaa',
                    'password': 'dgbcdaegb',
                    'email': 'beagfbbjig',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.usersUpdateManyMutationInput,
        where: types.usersWhereInput,
    ) -> int:
        """Update multiple users records

        Parameters
        ----------
        data
            users data to update the selected users records to
        where
            Filter to select the users records to update

        Returns
        -------
        int
            The total number of users records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all users records
        total = users.prisma().update_many(
            data={
                'userid': 909024364
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
    ) -> int:
        """Count the number of users records present in the database

        Parameters
        ----------
        select
            Select the users fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            users filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usersCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = users.prisma().count()

        # results: prisma.types.usersCountAggregateOutput
        results = users.prisma().count(
            select={
                '_all': True,
                'username': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.usersCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
    ) -> types.usersCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.usersCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.usersWhereInput] = None,
        cursor: Optional[types.usersWhereUniqueInput] = None,
    ) -> Union[int, types.usersCountAggregateOutput]:
        """Count the number of users records present in the database

        Parameters
        ----------
        select
            Select the users fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            users filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usersCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = users.prisma().count()

        # results: prisma.types.usersCountAggregateOutput
        results = users.prisma().count(
            select={
                '_all': True,
                'password': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.usersCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.usersWhereInput] = None
    ) -> int:
        """Delete multiple users records.

        Parameters
        ----------
        where
            Optional users filter to find the records to be deleted

        Returns
        -------
        int
            The total number of users records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all users records
        total = users.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.usersScalarFieldKeys'],
        *,
        where: Optional['types.usersWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.usersAvgAggregateInput'] = None,
        sum: Optional['types.usersSumAggregateInput'] = None,
        min: Optional['types.usersMinAggregateInput'] = None,
        max: Optional['types.usersMaxAggregateInput'] = None,
        having: Optional['types.usersScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.usersCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.usersScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.usersScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.usersGroupByOutput']:
        """Group users records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar users fields to group records by
        where
            users filter to select records
        take
            Limit the maximum number of users records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.usersGroupByOutput]
            A list of dictionaries representing the users record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group users records by email values
        # and count how many records are in each group
        results = users.prisma().group_by(
            ['email'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class userstreaksActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.userstreaks]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = userstreaks.prisma().query_raw(
            'SELECT * FROM userstreaks WHERE streakid = $1',
            755610165,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.userstreaks
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = userstreaks.prisma().query_first(
            'SELECT * FROM userstreaks WHERE userid = $1',
            1802188490,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.userstreaksCreateInput,
        include: Optional[types.userstreaksInclude] = None
    ) -> _PrismaModelT:
        """Create a new userstreaks record.

        Parameters
        ----------
        data
            userstreaks record data
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The created userstreaks record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a userstreaks record from just the required fields
        userstreaks = userstreaks.prisma().create(
            data={
                # data to create a userstreaks record
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.userstreaksCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple userstreaks records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of userstreaks record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = userstreaks.prisma().create_many(
            data=[
                {
                    # data to create a userstreaks record
                },
                {
                    # data to create a userstreaks record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.userstreaksWhereUniqueInput,
        include: Optional[types.userstreaksInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single userstreaks record.

        Parameters
        ----------
        where
            userstreaks filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The deleted userstreaks record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userstreaks = userstreaks.prisma().delete(
            where={
                'streakid': 294916155,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.userstreaksWhereUniqueInput,
        include: Optional[types.userstreaksInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique userstreaks record.

        Parameters
        ----------
        where
            userstreaks filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The found userstreaks record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userstreaks = userstreaks.prisma().find_unique(
            where={
                'streakid': 564073304,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.userstreaksWhereUniqueInput,
        include: Optional[types.userstreaksInclude] = None
    ) -> _PrismaModelT:
        """Find a unique userstreaks record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            userstreaks filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The found userstreaks record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userstreaks = userstreaks.prisma().find_unique_or_raise(
            where={
                'streakid': 380648625,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
        include: Optional[types.userstreaksInclude] = None,
        order: Optional[Union[types.userstreaksOrderByInput, List[types.userstreaksOrderByInput]]] = None,
        distinct: Optional[List[types.userstreaksScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple userstreaks records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of userstreaks records returned
        skip
            Ignore the first N results
        where
            userstreaks filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userstreaks model
        order
            Order the returned userstreaks records by any field
        distinct
            Filter userstreaks records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.userstreaks]
            The list of all userstreaks records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 userstreaks records
        userstreaks = userstreaks.prisma().find_many(take=10)

        # find the first 5 userstreaks records ordered by the currentstreak field
        userstreaks = userstreaks.prisma().find_many(
            take=5,
            order={
                'currentstreak': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
        include: Optional[types.userstreaksInclude] = None,
        order: Optional[Union[types.userstreaksOrderByInput, List[types.userstreaksOrderByInput]]] = None,
        distinct: Optional[List[types.userstreaksScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single userstreaks record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userstreaks filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userstreaks model
        order
            Order the returned userstreaks records by any field
        distinct
            Filter userstreaks records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userstreaks
            The first userstreaks record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userstreaks record ordered by the lasttestdate field
        userstreaks = userstreaks.prisma().find_first(
            skip=1,
            order={
                'lasttestdate': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
        include: Optional[types.userstreaksInclude] = None,
        order: Optional[Union[types.userstreaksOrderByInput, List[types.userstreaksOrderByInput]]] = None,
        distinct: Optional[List[types.userstreaksScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single userstreaks record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            userstreaks filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned userstreaks model
        order
            Order the returned userstreaks records by any field
        distinct
            Filter userstreaks records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.userstreaks
            The first userstreaks record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second userstreaks record ordered by the higheststreak field
        userstreaks = userstreaks.prisma().find_first_or_raise(
            skip=1,
            order={
                'higheststreak': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.userstreaksUpdateInput,
        where: types.userstreaksWhereUniqueInput,
        include: Optional[types.userstreaksInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single userstreaks record.

        Parameters
        ----------
        data
            userstreaks record data specifying what to update
        where
            userstreaks filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The updated userstreaks record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userstreaks = userstreaks.prisma().update(
            where={
                'streakid': 1030616470,
            },
            data={
                # data to update the userstreaks record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.userstreaksWhereUniqueInput,
        data: types.userstreaksUpsertInput,
        include: Optional[types.userstreaksInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            userstreaks filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned userstreaks model

        Returns
        -------
        prisma.models.userstreaks
            The created or updated userstreaks record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userstreaks = userstreaks.prisma().upsert(
            where={
                'streakid': 816411927,
            },
            data={
                'create': {
                    'streakid': 816411927,
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.userstreaksUpdateManyMutationInput,
        where: types.userstreaksWhereInput,
    ) -> int:
        """Update multiple userstreaks records

        Parameters
        ----------
        data
            userstreaks data to update the selected userstreaks records to
        where
            Filter to select the userstreaks records to update

        Returns
        -------
        int
            The total number of userstreaks records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all userstreaks records
        total = userstreaks.prisma().update_many(
            data={
                'streakid': 1084099844
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
    ) -> int:
        """Count the number of userstreaks records present in the database

        Parameters
        ----------
        select
            Select the userstreaks fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userstreaks filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userstreaksCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userstreaks.prisma().count()

        # results: prisma.types.userstreaksCountAggregateOutput
        results = userstreaks.prisma().count(
            select={
                '_all': True,
                'userid': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.userstreaksCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
    ) -> types.userstreaksCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.userstreaksCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userstreaksWhereInput] = None,
        cursor: Optional[types.userstreaksWhereUniqueInput] = None,
    ) -> Union[int, types.userstreaksCountAggregateOutput]:
        """Count the number of userstreaks records present in the database

        Parameters
        ----------
        select
            Select the userstreaks fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            userstreaks filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userstreaksCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = userstreaks.prisma().count()

        # results: prisma.types.userstreaksCountAggregateOutput
        results = userstreaks.prisma().count(
            select={
                '_all': True,
                'currentstreak': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.userstreaksCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.userstreaksWhereInput] = None
    ) -> int:
        """Delete multiple userstreaks records.

        Parameters
        ----------
        where
            Optional userstreaks filter to find the records to be deleted

        Returns
        -------
        int
            The total number of userstreaks records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all userstreaks records
        total = userstreaks.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.userstreaksScalarFieldKeys'],
        *,
        where: Optional['types.userstreaksWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.userstreaksAvgAggregateInput'] = None,
        sum: Optional['types.userstreaksSumAggregateInput'] = None,
        min: Optional['types.userstreaksMinAggregateInput'] = None,
        max: Optional['types.userstreaksMaxAggregateInput'] = None,
        having: Optional['types.userstreaksScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.userstreaksCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.userstreaksScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.userstreaksScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.userstreaksGroupByOutput']:
        """Group userstreaks records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar userstreaks fields to group records by
        where
            userstreaks filter to select records
        take
            Limit the maximum number of userstreaks records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.userstreaksGroupByOutput]
            A list of dictionaries representing the userstreaks record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group userstreaks records by lasttestdate values
        # and count how many records are in each group
        results = userstreaks.prisma().group_by(
            ['lasttestdate'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models