# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class ProblemsSet(bases.BaseProblemsSet):
    """Represents a ProblemsSet record"""

    problemsSetId: _int
    sectionid: _int
    examtypeid: _int
    title: _str
    type: _str
    content: 'fields.Json'
    mocksectionid: Optional[_int] = None
    mockquestionnumber: Optional[_int] = None
    examtypes: Optional['models.examtypes'] = None
    mocksections: Optional['models.mocksections'] = None
    sections: Optional['models.sections'] = None
    problems: Optional[List['models.problems']] = None
    problemssettags: Optional[List['models.problemssettags']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProblemsSetKeys']] = None,
        exclude: Optional[Iterable['types.ProblemsSetKeys']] = None,
        required: Optional[Iterable['types.ProblemsSetKeys']] = None,
        optional: Optional[Iterable['types.ProblemsSetKeys']] = None,
        relations: Optional[Mapping['types.ProblemsSetRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProblemsSetKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProblemsSet_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProblemsSet_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProblemsSet_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProblemsSet_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProblemsSet_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProblemsSet_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProblemsSet', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProblemsSet / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProblemsSet',
            }
        )
        _created_partial_types.add(name)


class examtypes(bases.Baseexamtypes):
    """Represents a examtypes record"""

    examtypeid: _int
    name: _str
    description: Optional[_str] = None
    ProblemsSet: Optional[List['models.ProblemsSet']] = None
    mocktests: Optional[List['models.mocktests']] = None
    problems: Optional[List['models.problems']] = None
    sections: Optional[List['models.sections']] = None
    tags: Optional[List['models.tags']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.examtypesKeys']] = None,
        exclude: Optional[Iterable['types.examtypesKeys']] = None,
        required: Optional[Iterable['types.examtypesKeys']] = None,
        optional: Optional[Iterable['types.examtypesKeys']] = None,
        relations: Optional[Mapping['types.examtypesRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.examtypesKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _examtypes_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _examtypes_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _examtypes_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _examtypes_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _examtypes_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _examtypes_relational_fields:
                        raise errors.UnknownRelationalFieldError('examtypes', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid examtypes / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'examtypes',
            }
        )
        _created_partial_types.add(name)


class mocktestrankings(bases.Basemocktestrankings):
    """Represents a mocktestrankings record"""

    rankingid: _int
    userid: Optional[_int] = None
    mocktestid: Optional[_int] = None
    rank: Optional[_int] = None
    percentile: Optional[decimal.Decimal] = None
    mocktests: Optional['models.mocktests'] = None
    users: Optional['models.users'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.mocktestrankingsKeys']] = None,
        exclude: Optional[Iterable['types.mocktestrankingsKeys']] = None,
        required: Optional[Iterable['types.mocktestrankingsKeys']] = None,
        optional: Optional[Iterable['types.mocktestrankingsKeys']] = None,
        relations: Optional[Mapping['types.mocktestrankingsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.mocktestrankingsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _mocktestrankings_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _mocktestrankings_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _mocktestrankings_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _mocktestrankings_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _mocktestrankings_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _mocktestrankings_relational_fields:
                        raise errors.UnknownRelationalFieldError('mocktestrankings', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid mocktestrankings / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'mocktestrankings',
            }
        )
        _created_partial_types.add(name)


class mocktests(bases.Basemocktests):
    """Represents a mocktests record"""

    mocktestid: _int
    difficulty: _int
    examtypeid: Optional[_int] = None
    date: datetime.datetime
    starttime: datetime.datetime
    endtime: datetime.datetime
    isactive: Optional[_bool] = None
    mocksections: Optional[List['models.mocksections']] = None
    mocktestrankings: Optional[List['models.mocktestrankings']] = None
    examtypes: Optional['models.examtypes'] = None
    usermocktestattempts: Optional[List['models.usermocktestattempts']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.mocktestsKeys']] = None,
        exclude: Optional[Iterable['types.mocktestsKeys']] = None,
        required: Optional[Iterable['types.mocktestsKeys']] = None,
        optional: Optional[Iterable['types.mocktestsKeys']] = None,
        relations: Optional[Mapping['types.mocktestsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.mocktestsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _mocktests_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _mocktests_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _mocktests_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _mocktests_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _mocktests_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _mocktests_relational_fields:
                        raise errors.UnknownRelationalFieldError('mocktests', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid mocktests / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'mocktests',
            }
        )
        _created_partial_types.add(name)


class mocktestsectionscores(bases.Basemocktestsectionscores):
    """Represents a mocktestsectionscores record"""

    scoreid: _int
    attemptid: Optional[_int] = None
    sectionid: Optional[_int] = None
    score: Optional[decimal.Decimal] = None
    usermocktestattempts: Optional['models.usermocktestattempts'] = None
    sections: Optional['models.sections'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.mocktestsectionscoresKeys']] = None,
        exclude: Optional[Iterable['types.mocktestsectionscoresKeys']] = None,
        required: Optional[Iterable['types.mocktestsectionscoresKeys']] = None,
        optional: Optional[Iterable['types.mocktestsectionscoresKeys']] = None,
        relations: Optional[Mapping['types.mocktestsectionscoresRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.mocktestsectionscoresKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _mocktestsectionscores_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _mocktestsectionscores_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _mocktestsectionscores_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _mocktestsectionscores_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _mocktestsectionscores_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _mocktestsectionscores_relational_fields:
                        raise errors.UnknownRelationalFieldError('mocktestsectionscores', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid mocktestsectionscores / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'mocktestsectionscores',
            }
        )
        _created_partial_types.add(name)


class mocksections(bases.Basemocksections):
    """Represents a mocksections record"""

    mocksectionid: _int
    mocktestid: _int
    sectionnumber: _int
    sectionid: _int
    problemsset: Optional[List['models.ProblemsSet']] = None
    mocktests: Optional['models.mocktests'] = None
    sections: Optional['models.sections'] = None
    problems: Optional[List['models.problems']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.mocksectionsKeys']] = None,
        exclude: Optional[Iterable['types.mocksectionsKeys']] = None,
        required: Optional[Iterable['types.mocksectionsKeys']] = None,
        optional: Optional[Iterable['types.mocksectionsKeys']] = None,
        relations: Optional[Mapping['types.mocksectionsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.mocksectionsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _mocksections_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _mocksections_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _mocksections_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _mocksections_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _mocksections_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _mocksections_relational_fields:
                        raise errors.UnknownRelationalFieldError('mocksections', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid mocksections / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'mocksections',
            }
        )
        _created_partial_types.add(name)


class performance(bases.Baseperformance):
    """Represents a performance record"""

    userid: _int
    sectionid: _int
    correctcount: Optional[_int] = None
    incorrectcount: Optional[_int] = None
    partialcorrectcount: Optional[_int] = None
    sections: Optional['models.sections'] = None
    users: Optional['models.users'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.performanceKeys']] = None,
        exclude: Optional[Iterable['types.performanceKeys']] = None,
        required: Optional[Iterable['types.performanceKeys']] = None,
        optional: Optional[Iterable['types.performanceKeys']] = None,
        relations: Optional[Mapping['types.performanceRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.performanceKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _performance_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _performance_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _performance_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _performance_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _performance_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _performance_relational_fields:
                        raise errors.UnknownRelationalFieldError('performance', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid performance / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'performance',
            }
        )
        _created_partial_types.add(name)


class problemoptions(bases.Baseproblemoptions):
    """Represents a problemoptions record"""

    optionid: _int
    problemid: Optional[_int] = None
    optiontext: _str
    iscorrect: Optional[_bool] = None
    group: Optional[_str] = None
    problems: Optional['models.problems'] = None
    userattempt_selectedoptions: Optional[List['models.userattempt_selectedoptions']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.problemoptionsKeys']] = None,
        exclude: Optional[Iterable['types.problemoptionsKeys']] = None,
        required: Optional[Iterable['types.problemoptionsKeys']] = None,
        optional: Optional[Iterable['types.problemoptionsKeys']] = None,
        relations: Optional[Mapping['types.problemoptionsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.problemoptionsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _problemoptions_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _problemoptions_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _problemoptions_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _problemoptions_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _problemoptions_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _problemoptions_relational_fields:
                        raise errors.UnknownRelationalFieldError('problemoptions', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid problemoptions / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'problemoptions',
            }
        )
        _created_partial_types.add(name)


class problems(bases.Baseproblems):
    """Represents a problems record"""

    type: Optional[_str] = None
    prompt: Optional[_str] = None
    problemid: _int
    sectionid: Optional[_int] = None
    examtypeid: Optional[_int] = None
    title: _str
    text: _str
    difficulty: Optional[_int] = None
    correctattemptscount: Optional[_int] = None
    totalattemptscount: Optional[_int] = None
    metadata: Optional['fields.Json'] = None
    creationdate: Optional[datetime.datetime] = None
    addedDate: Optional[datetime.datetime] = None
    isChildren: _bool
    problemsSetId: Optional[_int] = None
    mocksectionid: Optional[_int] = None
    isMockQuestion: _bool
    solution: Optional['fields.Json'] = None
    mockquestionnumber: Optional[_int] = None
    problemoptions: Optional[List['models.problemoptions']] = None
    examtypes: Optional['models.examtypes'] = None
    mocksections: Optional['models.mocksections'] = None
    ProblemsSet: Optional['models.ProblemsSet'] = None
    sections: Optional['models.sections'] = None
    problemtags: Optional[List['models.problemtags']] = None
    userattempts: Optional[List['models.userattempts']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.problemsKeys']] = None,
        exclude: Optional[Iterable['types.problemsKeys']] = None,
        required: Optional[Iterable['types.problemsKeys']] = None,
        optional: Optional[Iterable['types.problemsKeys']] = None,
        relations: Optional[Mapping['types.problemsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.problemsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _problems_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _problems_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _problems_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _problems_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _problems_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _problems_relational_fields:
                        raise errors.UnknownRelationalFieldError('problems', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid problems / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'problems',
            }
        )
        _created_partial_types.add(name)


class problemssettags(bases.Baseproblemssettags):
    """Represents a problemssettags record"""

    problemsSetId: _int
    tagid: _int
    ProblemsSet: Optional['models.ProblemsSet'] = None
    tags: Optional['models.tags'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.problemssettagsKeys']] = None,
        exclude: Optional[Iterable['types.problemssettagsKeys']] = None,
        required: Optional[Iterable['types.problemssettagsKeys']] = None,
        optional: Optional[Iterable['types.problemssettagsKeys']] = None,
        relations: Optional[Mapping['types.problemssettagsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.problemssettagsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _problemssettags_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _problemssettags_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _problemssettags_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _problemssettags_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _problemssettags_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _problemssettags_relational_fields:
                        raise errors.UnknownRelationalFieldError('problemssettags', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid problemssettags / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'problemssettags',
            }
        )
        _created_partial_types.add(name)


class problemtags(bases.Baseproblemtags):
    """Represents a problemtags record"""

    problemid: _int
    tagid: _int
    problems: Optional['models.problems'] = None
    tags: Optional['models.tags'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.problemtagsKeys']] = None,
        exclude: Optional[Iterable['types.problemtagsKeys']] = None,
        required: Optional[Iterable['types.problemtagsKeys']] = None,
        optional: Optional[Iterable['types.problemtagsKeys']] = None,
        relations: Optional[Mapping['types.problemtagsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.problemtagsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _problemtags_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _problemtags_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _problemtags_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _problemtags_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _problemtags_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _problemtags_relational_fields:
                        raise errors.UnknownRelationalFieldError('problemtags', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid problemtags / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'problemtags',
            }
        )
        _created_partial_types.add(name)


class sections(bases.Basesections):
    """Represents a sections record"""

    sectionid: _int
    examtypeid: Optional[_int] = None
    name: _str
    description: Optional[_str] = None
    ProblemsSet: Optional[List['models.ProblemsSet']] = None
    mocksections: Optional[List['models.mocksections']] = None
    mocktestsectionscores: Optional[List['models.mocktestsectionscores']] = None
    performance: Optional[List['models.performance']] = None
    problems: Optional[List['models.problems']] = None
    examtypes: Optional['models.examtypes'] = None
    tags: Optional[List['models.tags']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.sectionsKeys']] = None,
        exclude: Optional[Iterable['types.sectionsKeys']] = None,
        required: Optional[Iterable['types.sectionsKeys']] = None,
        optional: Optional[Iterable['types.sectionsKeys']] = None,
        relations: Optional[Mapping['types.sectionsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.sectionsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _sections_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _sections_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _sections_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _sections_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _sections_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _sections_relational_fields:
                        raise errors.UnknownRelationalFieldError('sections', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid sections / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'sections',
            }
        )
        _created_partial_types.add(name)


class tags(bases.Basetags):
    """Represents a tags record"""

    tagid: _int
    name: _str
    examtypeid: _int
    sectionid: _int
    problemssettags: Optional[List['models.problemssettags']] = None
    problemtags: Optional[List['models.problemtags']] = None
    examtypes: Optional['models.examtypes'] = None
    sections: Optional['models.sections'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.tagsKeys']] = None,
        exclude: Optional[Iterable['types.tagsKeys']] = None,
        required: Optional[Iterable['types.tagsKeys']] = None,
        optional: Optional[Iterable['types.tagsKeys']] = None,
        relations: Optional[Mapping['types.tagsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.tagsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _tags_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _tags_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _tags_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _tags_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _tags_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _tags_relational_fields:
                        raise errors.UnknownRelationalFieldError('tags', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid tags / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'tags',
            }
        )
        _created_partial_types.add(name)


class userattempt_selectedoptions(bases.Baseuserattempt_selectedoptions):
    """Represents a userattempt_selectedoptions record"""

    id: _int
    userattemptid: _int
    optionid: _int
    problemoptions: Optional['models.problemoptions'] = None
    userattempts: Optional['models.userattempts'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.userattempt_selectedoptionsKeys']] = None,
        exclude: Optional[Iterable['types.userattempt_selectedoptionsKeys']] = None,
        required: Optional[Iterable['types.userattempt_selectedoptionsKeys']] = None,
        optional: Optional[Iterable['types.userattempt_selectedoptionsKeys']] = None,
        relations: Optional[Mapping['types.userattempt_selectedoptionsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.userattempt_selectedoptionsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _userattempt_selectedoptions_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _userattempt_selectedoptions_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _userattempt_selectedoptions_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _userattempt_selectedoptions_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _userattempt_selectedoptions_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _userattempt_selectedoptions_relational_fields:
                        raise errors.UnknownRelationalFieldError('userattempt_selectedoptions', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid userattempt_selectedoptions / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'userattempt_selectedoptions',
            }
        )
        _created_partial_types.add(name)


class userattempts(bases.Baseuserattempts):
    """Represents a userattempts record"""

    attemptid: _int
    userid: Optional[_int] = None
    problemid: Optional[_int] = None
    timetaken: Optional[_str] = None
    attemptdate: Optional[datetime.datetime] = None
    partialcorrectnessscore: Optional[decimal.Decimal] = None
    iscorrect: Optional[_bool] = None
    userattempt_selectedoptions: Optional[List['models.userattempt_selectedoptions']] = None
    problems: Optional['models.problems'] = None
    users: Optional['models.users'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.userattemptsKeys']] = None,
        exclude: Optional[Iterable['types.userattemptsKeys']] = None,
        required: Optional[Iterable['types.userattemptsKeys']] = None,
        optional: Optional[Iterable['types.userattemptsKeys']] = None,
        relations: Optional[Mapping['types.userattemptsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.userattemptsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _userattempts_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _userattempts_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _userattempts_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _userattempts_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _userattempts_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _userattempts_relational_fields:
                        raise errors.UnknownRelationalFieldError('userattempts', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid userattempts / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'userattempts',
            }
        )
        _created_partial_types.add(name)


class usermocktestattempts(bases.Baseusermocktestattempts):
    """Represents a usermocktestattempts record"""

    attemptid: _int
    userid: Optional[_int] = None
    mocktestid: Optional[_int] = None
    starttime: datetime.datetime
    endtime: Optional[datetime.datetime] = None
    totalscore: Optional[decimal.Decimal] = None
    isofficialattempt: Optional[_bool] = None
    mocktestsectionscores: Optional[List['models.mocktestsectionscores']] = None
    mocktests: Optional['models.mocktests'] = None
    users: Optional['models.users'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.usermocktestattemptsKeys']] = None,
        exclude: Optional[Iterable['types.usermocktestattemptsKeys']] = None,
        required: Optional[Iterable['types.usermocktestattemptsKeys']] = None,
        optional: Optional[Iterable['types.usermocktestattemptsKeys']] = None,
        relations: Optional[Mapping['types.usermocktestattemptsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.usermocktestattemptsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _usermocktestattempts_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _usermocktestattempts_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _usermocktestattempts_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _usermocktestattempts_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _usermocktestattempts_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _usermocktestattempts_relational_fields:
                        raise errors.UnknownRelationalFieldError('usermocktestattempts', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid usermocktestattempts / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'usermocktestattempts',
            }
        )
        _created_partial_types.add(name)


class users(bases.Baseusers):
    """Represents a users record"""

    userid: _int
    username: _str
    password: _str
    email: _str
    registrationdate: Optional[datetime.datetime] = None
    mocktestrankings: Optional[List['models.mocktestrankings']] = None
    performance: Optional[List['models.performance']] = None
    userattempts: Optional[List['models.userattempts']] = None
    usermocktestattempts: Optional[List['models.usermocktestattempts']] = None
    userstreaks: Optional[List['models.userstreaks']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.usersKeys']] = None,
        exclude: Optional[Iterable['types.usersKeys']] = None,
        required: Optional[Iterable['types.usersKeys']] = None,
        optional: Optional[Iterable['types.usersKeys']] = None,
        relations: Optional[Mapping['types.usersRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.usersKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _users_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _users_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _users_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _users_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _users_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _users_relational_fields:
                        raise errors.UnknownRelationalFieldError('users', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid users / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'users',
            }
        )
        _created_partial_types.add(name)


class userstreaks(bases.Baseuserstreaks):
    """Represents a userstreaks record"""

    streakid: _int
    userid: Optional[_int] = None
    currentstreak: Optional[_int] = None
    lasttestdate: Optional[datetime.datetime] = None
    higheststreak: Optional[_int] = None
    users: Optional['models.users'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.userstreaksKeys']] = None,
        exclude: Optional[Iterable['types.userstreaksKeys']] = None,
        required: Optional[Iterable['types.userstreaksKeys']] = None,
        optional: Optional[Iterable['types.userstreaksKeys']] = None,
        relations: Optional[Mapping['types.userstreaksRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.userstreaksKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _userstreaks_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _userstreaks_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _userstreaks_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _userstreaks_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _userstreaks_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _userstreaks_relational_fields:
                        raise errors.UnknownRelationalFieldError('userstreaks', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid userstreaks / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'userstreaks',
            }
        )
        _created_partial_types.add(name)



_ProblemsSet_relational_fields: Set[str] = {
        'examtypes',
        'mocksections',
        'sections',
        'problems',
        'problemssettags',
    }
_ProblemsSet_fields: Dict['types.ProblemsSetKeys', PartialModelField] = OrderedDict(
    [
        ('problemsSetId', {
            'name': 'problemsSetId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocksectionid', {
            'name': 'mocksectionid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('mockquestionnumber', {
            'name': 'mockquestionnumber',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypes', {
            'name': 'examtypes',
            'is_list': False,
            'optional': True,
            'type': 'models.examtypes',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocksections', {
            'name': 'mocksections',
            'is_list': False,
            'optional': True,
            'type': 'models.mocksections',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problems\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('problemssettags', {
            'name': 'problemssettags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problemssettags\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_examtypes_relational_fields: Set[str] = {
        'ProblemsSet',
        'mocktests',
        'problems',
        'sections',
        'tags',
    }
_examtypes_fields: Dict['types.examtypesKeys', PartialModelField] = OrderedDict(
    [
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ProblemsSet', {
            'name': 'ProblemsSet',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProblemsSet\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocktests', {
            'name': 'mocktests',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocktests\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problems\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.sections\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('tags', {
            'name': 'tags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.tags\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_mocktestrankings_relational_fields: Set[str] = {
        'mocktests',
        'users',
    }
_mocktestrankings_fields: Dict['types.mocktestrankingsKeys', PartialModelField] = OrderedDict(
    [
        ('rankingid', {
            'name': 'rankingid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktestid', {
            'name': 'mocktestid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('rank', {
            'name': 'rank',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('percentile', {
            'name': 'percentile',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktests', {
            'name': 'mocktests',
            'is_list': False,
            'optional': True,
            'type': 'models.mocktests',
            'is_relational': True,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_mocktests_relational_fields: Set[str] = {
        'mocksections',
        'mocktestrankings',
        'examtypes',
        'usermocktestattempts',
    }
_mocktests_fields: Dict['types.mocktestsKeys', PartialModelField] = OrderedDict(
    [
        ('mocktestid', {
            'name': 'mocktestid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficulty', {
            'name': 'difficulty',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('date', {
            'name': 'date',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('starttime', {
            'name': 'starttime',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('endtime', {
            'name': 'endtime',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('isactive', {
            'name': 'isactive',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocksections', {
            'name': 'mocksections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocksections\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocktestrankings', {
            'name': 'mocktestrankings',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocktestrankings\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('examtypes', {
            'name': 'examtypes',
            'is_list': False,
            'optional': True,
            'type': 'models.examtypes',
            'is_relational': True,
            'documentation': None,
        }),
        ('usermocktestattempts', {
            'name': 'usermocktestattempts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.usermocktestattempts\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_mocktestsectionscores_relational_fields: Set[str] = {
        'usermocktestattempts',
        'sections',
    }
_mocktestsectionscores_fields: Dict['types.mocktestsectionscoresKeys', PartialModelField] = OrderedDict(
    [
        ('scoreid', {
            'name': 'scoreid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('attemptid', {
            'name': 'attemptid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('score', {
            'name': 'score',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('usermocktestattempts', {
            'name': 'usermocktestattempts',
            'is_list': False,
            'optional': True,
            'type': 'models.usermocktestattempts',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_mocksections_relational_fields: Set[str] = {
        'problemsset',
        'mocktests',
        'sections',
        'problems',
    }
_mocksections_fields: Dict['types.mocksectionsKeys', PartialModelField] = OrderedDict(
    [
        ('mocksectionid', {
            'name': 'mocksectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktestid', {
            'name': 'mocktestid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionnumber', {
            'name': 'sectionnumber',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemsset', {
            'name': 'problemsset',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProblemsSet\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocktests', {
            'name': 'mocktests',
            'is_list': False,
            'optional': True,
            'type': 'models.mocktests',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problems\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_performance_relational_fields: Set[str] = {
        'sections',
        'users',
    }
_performance_fields: Dict['types.performanceKeys', PartialModelField] = OrderedDict(
    [
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('correctcount', {
            'name': 'correctcount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('incorrectcount', {
            'name': 'incorrectcount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('partialcorrectcount', {
            'name': 'partialcorrectcount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_problemoptions_relational_fields: Set[str] = {
        'problems',
        'userattempt_selectedoptions',
    }
_problemoptions_fields: Dict['types.problemoptionsKeys', PartialModelField] = OrderedDict(
    [
        ('optionid', {
            'name': 'optionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemid', {
            'name': 'problemid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('optiontext', {
            'name': 'optiontext',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('iscorrect', {
            'name': 'iscorrect',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('group', {
            'name': 'group',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': False,
            'optional': True,
            'type': 'models.problems',
            'is_relational': True,
            'documentation': None,
        }),
        ('userattempt_selectedoptions', {
            'name': 'userattempt_selectedoptions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.userattempt_selectedoptions\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_problems_relational_fields: Set[str] = {
        'problemoptions',
        'examtypes',
        'mocksections',
        'ProblemsSet',
        'sections',
        'problemtags',
        'userattempts',
    }
_problems_fields: Dict['types.problemsKeys', PartialModelField] = OrderedDict(
    [
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('prompt', {
            'name': 'prompt',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemid', {
            'name': 'problemid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('text', {
            'name': 'text',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficulty', {
            'name': 'difficulty',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('correctattemptscount', {
            'name': 'correctattemptscount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalattemptscount', {
            'name': 'totalattemptscount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('creationdate', {
            'name': 'creationdate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('addedDate', {
            'name': 'addedDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('isChildren', {
            'name': 'isChildren',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemsSetId', {
            'name': 'problemsSetId',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocksectionid', {
            'name': 'mocksectionid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('isMockQuestion', {
            'name': 'isMockQuestion',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('solution', {
            'name': 'solution',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('mockquestionnumber', {
            'name': 'mockquestionnumber',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemoptions', {
            'name': 'problemoptions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problemoptions\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('examtypes', {
            'name': 'examtypes',
            'is_list': False,
            'optional': True,
            'type': 'models.examtypes',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocksections', {
            'name': 'mocksections',
            'is_list': False,
            'optional': True,
            'type': 'models.mocksections',
            'is_relational': True,
            'documentation': None,
        }),
        ('ProblemsSet', {
            'name': 'ProblemsSet',
            'is_list': False,
            'optional': True,
            'type': 'models.ProblemsSet',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
        ('problemtags', {
            'name': 'problemtags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problemtags\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userattempts', {
            'name': 'userattempts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.userattempts\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_problemssettags_relational_fields: Set[str] = {
        'ProblemsSet',
        'tags',
    }
_problemssettags_fields: Dict['types.problemssettagsKeys', PartialModelField] = OrderedDict(
    [
        ('problemsSetId', {
            'name': 'problemsSetId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('tagid', {
            'name': 'tagid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('ProblemsSet', {
            'name': 'ProblemsSet',
            'is_list': False,
            'optional': True,
            'type': 'models.ProblemsSet',
            'is_relational': True,
            'documentation': None,
        }),
        ('tags', {
            'name': 'tags',
            'is_list': False,
            'optional': True,
            'type': 'models.tags',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_problemtags_relational_fields: Set[str] = {
        'problems',
        'tags',
    }
_problemtags_fields: Dict['types.problemtagsKeys', PartialModelField] = OrderedDict(
    [
        ('problemid', {
            'name': 'problemid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('tagid', {
            'name': 'tagid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': False,
            'optional': True,
            'type': 'models.problems',
            'is_relational': True,
            'documentation': None,
        }),
        ('tags', {
            'name': 'tags',
            'is_list': False,
            'optional': True,
            'type': 'models.tags',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_sections_relational_fields: Set[str] = {
        'ProblemsSet',
        'mocksections',
        'mocktestsectionscores',
        'performance',
        'problems',
        'examtypes',
        'tags',
    }
_sections_fields: Dict['types.sectionsKeys', PartialModelField] = OrderedDict(
    [
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ProblemsSet', {
            'name': 'ProblemsSet',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProblemsSet\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocksections', {
            'name': 'mocksections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocksections\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocktestsectionscores', {
            'name': 'mocktestsectionscores',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocktestsectionscores\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('performance', {
            'name': 'performance',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.performance\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problems\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('examtypes', {
            'name': 'examtypes',
            'is_list': False,
            'optional': True,
            'type': 'models.examtypes',
            'is_relational': True,
            'documentation': None,
        }),
        ('tags', {
            'name': 'tags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.tags\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_tags_relational_fields: Set[str] = {
        'problemssettags',
        'problemtags',
        'examtypes',
        'sections',
    }
_tags_fields: Dict['types.tagsKeys', PartialModelField] = OrderedDict(
    [
        ('tagid', {
            'name': 'tagid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('examtypeid', {
            'name': 'examtypeid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionid', {
            'name': 'sectionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemssettags', {
            'name': 'problemssettags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problemssettags\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('problemtags', {
            'name': 'problemtags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.problemtags\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('examtypes', {
            'name': 'examtypes',
            'is_list': False,
            'optional': True,
            'type': 'models.examtypes',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': False,
            'optional': True,
            'type': 'models.sections',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_userattempt_selectedoptions_relational_fields: Set[str] = {
        'problemoptions',
        'userattempts',
    }
_userattempt_selectedoptions_fields: Dict['types.userattempt_selectedoptionsKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('userattemptid', {
            'name': 'userattemptid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('optionid', {
            'name': 'optionid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemoptions', {
            'name': 'problemoptions',
            'is_list': False,
            'optional': True,
            'type': 'models.problemoptions',
            'is_relational': True,
            'documentation': None,
        }),
        ('userattempts', {
            'name': 'userattempts',
            'is_list': False,
            'optional': True,
            'type': 'models.userattempts',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_userattempts_relational_fields: Set[str] = {
        'userattempt_selectedoptions',
        'problems',
        'users',
    }
_userattempts_fields: Dict['types.userattemptsKeys', PartialModelField] = OrderedDict(
    [
        ('attemptid', {
            'name': 'attemptid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('problemid', {
            'name': 'problemid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('timetaken', {
            'name': 'timetaken',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('attemptdate', {
            'name': 'attemptdate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('partialcorrectnessscore', {
            'name': 'partialcorrectnessscore',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('iscorrect', {
            'name': 'iscorrect',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('userattempt_selectedoptions', {
            'name': 'userattempt_selectedoptions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.userattempt_selectedoptions\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('problems', {
            'name': 'problems',
            'is_list': False,
            'optional': True,
            'type': 'models.problems',
            'is_relational': True,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_usermocktestattempts_relational_fields: Set[str] = {
        'mocktestsectionscores',
        'mocktests',
        'users',
    }
_usermocktestattempts_fields: Dict['types.usermocktestattemptsKeys', PartialModelField] = OrderedDict(
    [
        ('attemptid', {
            'name': 'attemptid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktestid', {
            'name': 'mocktestid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('starttime', {
            'name': 'starttime',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('endtime', {
            'name': 'endtime',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalscore', {
            'name': 'totalscore',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('isofficialattempt', {
            'name': 'isofficialattempt',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktestsectionscores', {
            'name': 'mocktestsectionscores',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocktestsectionscores\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('mocktests', {
            'name': 'mocktests',
            'is_list': False,
            'optional': True,
            'type': 'models.mocktests',
            'is_relational': True,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_users_relational_fields: Set[str] = {
        'mocktestrankings',
        'performance',
        'userattempts',
        'usermocktestattempts',
        'userstreaks',
    }
_users_fields: Dict['types.usersKeys', PartialModelField] = OrderedDict(
    [
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('username', {
            'name': 'username',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('password', {
            'name': 'password',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('registrationdate', {
            'name': 'registrationdate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('mocktestrankings', {
            'name': 'mocktestrankings',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.mocktestrankings\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('performance', {
            'name': 'performance',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.performance\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userattempts', {
            'name': 'userattempts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.userattempts\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('usermocktestattempts', {
            'name': 'usermocktestattempts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.usermocktestattempts\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userstreaks', {
            'name': 'userstreaks',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.userstreaks\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_userstreaks_relational_fields: Set[str] = {
        'users',
    }
_userstreaks_fields: Dict['types.userstreaksKeys', PartialModelField] = OrderedDict(
    [
        ('streakid', {
            'name': 'streakid',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('userid', {
            'name': 'userid',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('currentstreak', {
            'name': 'currentstreak',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('lasttestdate', {
            'name': 'lasttestdate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('higheststreak', {
            'name': 'higheststreak',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(ProblemsSet)
model_rebuild(examtypes)
model_rebuild(mocktestrankings)
model_rebuild(mocktests)
model_rebuild(mocktestsectionscores)
model_rebuild(mocksections)
model_rebuild(performance)
model_rebuild(problemoptions)
model_rebuild(problems)
model_rebuild(problemssettags)
model_rebuild(problemtags)
model_rebuild(sections)
model_rebuild(tags)
model_rebuild(userattempt_selectedoptions)
model_rebuild(userattempts)
model_rebuild(usermocktestattempts)
model_rebuild(users)
model_rebuild(userstreaks)
