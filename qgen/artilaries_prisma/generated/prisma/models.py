# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Category(bases.BaseCategory):
    """Represents a Category record"""

    id: _int
    name: _str
    difficultyDescriptions: Optional[List['models.DifficultyDescription']] = None
    sections: Optional[List['models.Section']] = None
    complexityGuidelines: Optional[List['models.ComplexityGuideline']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CategoryKeys']] = None,
        exclude: Optional[Iterable['types.CategoryKeys']] = None,
        required: Optional[Iterable['types.CategoryKeys']] = None,
        optional: Optional[Iterable['types.CategoryKeys']] = None,
        relations: Optional[Mapping['types.CategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Category_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Category_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Category_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Category_relational_fields:
                        raise errors.UnknownRelationalFieldError('Category', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Category / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Category',
            }
        )
        _created_partial_types.add(name)


class DifficultyLevel(bases.BaseDifficultyLevel):
    """Represents a DifficultyLevel record"""

    id: _int
    level: _int
    difficultyDescriptions: Optional[List['models.DifficultyDescription']] = None
    vocabulary: Optional[List['models.Vocabulary']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DifficultyLevelKeys']] = None,
        exclude: Optional[Iterable['types.DifficultyLevelKeys']] = None,
        required: Optional[Iterable['types.DifficultyLevelKeys']] = None,
        optional: Optional[Iterable['types.DifficultyLevelKeys']] = None,
        relations: Optional[Mapping['types.DifficultyLevelRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DifficultyLevelKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DifficultyLevel_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DifficultyLevel_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DifficultyLevel_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DifficultyLevel_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DifficultyLevel_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DifficultyLevel_relational_fields:
                        raise errors.UnknownRelationalFieldError('DifficultyLevel', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DifficultyLevel / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DifficultyLevel',
            }
        )
        _created_partial_types.add(name)


class DifficultyDescription(bases.BaseDifficultyDescription):
    """Represents a DifficultyDescription record"""

    id: _int
    categoryId: _int
    difficultyLevelId: _int
    description: _str
    category: Optional['models.Category'] = None
    difficultyLevel: Optional['models.DifficultyLevel'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DifficultyDescriptionKeys']] = None,
        exclude: Optional[Iterable['types.DifficultyDescriptionKeys']] = None,
        required: Optional[Iterable['types.DifficultyDescriptionKeys']] = None,
        optional: Optional[Iterable['types.DifficultyDescriptionKeys']] = None,
        relations: Optional[Mapping['types.DifficultyDescriptionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DifficultyDescriptionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DifficultyDescription_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DifficultyDescription_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DifficultyDescription_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DifficultyDescription_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DifficultyDescription_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DifficultyDescription_relational_fields:
                        raise errors.UnknownRelationalFieldError('DifficultyDescription', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DifficultyDescription / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DifficultyDescription',
            }
        )
        _created_partial_types.add(name)


class Exam(bases.BaseExam):
    """Represents a Exam record"""

    id: _int
    name: _str
    stages: Optional[List['models.Stage']] = None
    vocabulary: Optional[List['models.Vocabulary']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ExamKeys']] = None,
        exclude: Optional[Iterable['types.ExamKeys']] = None,
        required: Optional[Iterable['types.ExamKeys']] = None,
        optional: Optional[Iterable['types.ExamKeys']] = None,
        relations: Optional[Mapping['types.ExamRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ExamKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Exam_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Exam_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Exam_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Exam_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Exam_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Exam_relational_fields:
                        raise errors.UnknownRelationalFieldError('Exam', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Exam / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Exam',
            }
        )
        _created_partial_types.add(name)


class Stage(bases.BaseStage):
    """Represents a Stage record"""

    id: _int
    examId: _int
    stageNumber: _int
    exam: Optional['models.Exam'] = None
    sections: Optional[List['models.Section']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StageKeys']] = None,
        exclude: Optional[Iterable['types.StageKeys']] = None,
        required: Optional[Iterable['types.StageKeys']] = None,
        optional: Optional[Iterable['types.StageKeys']] = None,
        relations: Optional[Mapping['types.StageRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StageKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Stage_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Stage_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Stage_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Stage_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Stage_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Stage_relational_fields:
                        raise errors.UnknownRelationalFieldError('Stage', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Stage / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Stage',
            }
        )
        _created_partial_types.add(name)


class Section(bases.BaseSection):
    """Represents a Section record"""

    id: _int
    stageId: _int
    categoryId: _int
    totalQuestions: _int
    stage: Optional['models.Stage'] = None
    category: Optional['models.Category'] = None
    typeCounts: Optional[List['models.SectionQuestionTypeCount']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SectionKeys']] = None,
        exclude: Optional[Iterable['types.SectionKeys']] = None,
        required: Optional[Iterable['types.SectionKeys']] = None,
        optional: Optional[Iterable['types.SectionKeys']] = None,
        relations: Optional[Mapping['types.SectionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SectionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Section_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Section_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Section_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Section_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Section_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Section_relational_fields:
                        raise errors.UnknownRelationalFieldError('Section', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Section / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Section',
            }
        )
        _created_partial_types.add(name)


class QuestionType(bases.BaseQuestionType):
    """Represents a QuestionType record"""

    id: _int
    name: _str
    type: _str
    options: _str
    nomenclature: _str
    systemInstructionTemplate: _str
    sectionCounts: Optional[List['models.SectionQuestionTypeCount']] = None
    promptComponents: Optional[List['models.QuestionTypePromptComponent']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuestionTypeKeys']] = None,
        exclude: Optional[Iterable['types.QuestionTypeKeys']] = None,
        required: Optional[Iterable['types.QuestionTypeKeys']] = None,
        optional: Optional[Iterable['types.QuestionTypeKeys']] = None,
        relations: Optional[Mapping['types.QuestionTypeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuestionTypeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _QuestionType_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _QuestionType_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _QuestionType_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _QuestionType_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _QuestionType_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _QuestionType_relational_fields:
                        raise errors.UnknownRelationalFieldError('QuestionType', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid QuestionType / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'QuestionType',
            }
        )
        _created_partial_types.add(name)


class SectionQuestionTypeCount(bases.BaseSectionQuestionTypeCount):
    """Represents a SectionQuestionTypeCount record"""

    id: _int
    sectionId: _int
    questionTypeId: _int
    count: _int
    section: Optional['models.Section'] = None
    questionType: Optional['models.QuestionType'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SectionQuestionTypeCountKeys']] = None,
        exclude: Optional[Iterable['types.SectionQuestionTypeCountKeys']] = None,
        required: Optional[Iterable['types.SectionQuestionTypeCountKeys']] = None,
        optional: Optional[Iterable['types.SectionQuestionTypeCountKeys']] = None,
        relations: Optional[Mapping['types.SectionQuestionTypeCountRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SectionQuestionTypeCountKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SectionQuestionTypeCount_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SectionQuestionTypeCount_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SectionQuestionTypeCount_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SectionQuestionTypeCount_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SectionQuestionTypeCount_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SectionQuestionTypeCount_relational_fields:
                        raise errors.UnknownRelationalFieldError('SectionQuestionTypeCount', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SectionQuestionTypeCount / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SectionQuestionTypeCount',
            }
        )
        _created_partial_types.add(name)


class PromptComponent(bases.BasePromptComponent):
    """Represents a PromptComponent record"""

    id: _int
    name: _str
    values: Optional[List['models.PromptComponentValue']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PromptComponentKeys']] = None,
        exclude: Optional[Iterable['types.PromptComponentKeys']] = None,
        required: Optional[Iterable['types.PromptComponentKeys']] = None,
        optional: Optional[Iterable['types.PromptComponentKeys']] = None,
        relations: Optional[Mapping['types.PromptComponentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PromptComponentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PromptComponent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PromptComponent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PromptComponent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PromptComponent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PromptComponent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PromptComponent_relational_fields:
                        raise errors.UnknownRelationalFieldError('PromptComponent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PromptComponent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PromptComponent',
            }
        )
        _created_partial_types.add(name)


class PromptComponentValue(bases.BasePromptComponentValue):
    """Represents a PromptComponentValue record"""

    id: _int
    componentId: _int
    value: _str
    component: Optional['models.PromptComponent'] = None
    questionTypes: Optional[List['models.QuestionTypePromptComponent']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PromptComponentValueKeys']] = None,
        exclude: Optional[Iterable['types.PromptComponentValueKeys']] = None,
        required: Optional[Iterable['types.PromptComponentValueKeys']] = None,
        optional: Optional[Iterable['types.PromptComponentValueKeys']] = None,
        relations: Optional[Mapping['types.PromptComponentValueRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PromptComponentValueKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PromptComponentValue_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PromptComponentValue_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PromptComponentValue_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PromptComponentValue_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PromptComponentValue_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PromptComponentValue_relational_fields:
                        raise errors.UnknownRelationalFieldError('PromptComponentValue', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PromptComponentValue / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PromptComponentValue',
            }
        )
        _created_partial_types.add(name)


class QuestionTypePromptComponent(bases.BaseQuestionTypePromptComponent):
    """Represents a QuestionTypePromptComponent record"""

    id: _int
    questionTypeId: _int
    componentValueId: _int
    questionType: Optional['models.QuestionType'] = None
    componentValue: Optional['models.PromptComponentValue'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuestionTypePromptComponentKeys']] = None,
        exclude: Optional[Iterable['types.QuestionTypePromptComponentKeys']] = None,
        required: Optional[Iterable['types.QuestionTypePromptComponentKeys']] = None,
        optional: Optional[Iterable['types.QuestionTypePromptComponentKeys']] = None,
        relations: Optional[Mapping['types.QuestionTypePromptComponentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuestionTypePromptComponentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _QuestionTypePromptComponent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _QuestionTypePromptComponent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _QuestionTypePromptComponent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _QuestionTypePromptComponent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _QuestionTypePromptComponent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _QuestionTypePromptComponent_relational_fields:
                        raise errors.UnknownRelationalFieldError('QuestionTypePromptComponent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid QuestionTypePromptComponent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'QuestionTypePromptComponent',
            }
        )
        _created_partial_types.add(name)


class Vocabulary(bases.BaseVocabulary):
    """Represents a Vocabulary record"""

    id: _int
    word: _str
    definition: Optional[_str] = None
    examId: _int
    difficultyLevelId: _int
    exam: Optional['models.Exam'] = None
    difficultyLevel: Optional['models.DifficultyLevel'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.VocabularyKeys']] = None,
        exclude: Optional[Iterable['types.VocabularyKeys']] = None,
        required: Optional[Iterable['types.VocabularyKeys']] = None,
        optional: Optional[Iterable['types.VocabularyKeys']] = None,
        relations: Optional[Mapping['types.VocabularyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.VocabularyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Vocabulary_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Vocabulary_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Vocabulary_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Vocabulary_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Vocabulary_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Vocabulary_relational_fields:
                        raise errors.UnknownRelationalFieldError('Vocabulary', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Vocabulary / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Vocabulary',
            }
        )
        _created_partial_types.add(name)


class ComplexityGuideline(bases.BaseComplexityGuideline):
    """Represents a ComplexityGuideline record"""

    id: _int
    categoryId: _int
    guideline: _str
    category: Optional['models.Category'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ComplexityGuidelineKeys']] = None,
        exclude: Optional[Iterable['types.ComplexityGuidelineKeys']] = None,
        required: Optional[Iterable['types.ComplexityGuidelineKeys']] = None,
        optional: Optional[Iterable['types.ComplexityGuidelineKeys']] = None,
        relations: Optional[Mapping['types.ComplexityGuidelineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ComplexityGuidelineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ComplexityGuideline_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ComplexityGuideline_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ComplexityGuideline_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ComplexityGuideline_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ComplexityGuideline_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ComplexityGuideline_relational_fields:
                        raise errors.UnknownRelationalFieldError('ComplexityGuideline', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ComplexityGuideline / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ComplexityGuideline',
            }
        )
        _created_partial_types.add(name)


class SystemTemplate(bases.BaseSystemTemplate):
    """Represents a SystemTemplate record"""

    id: _int
    name: _str
    content: _str

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SystemTemplateKeys']] = None,
        exclude: Optional[Iterable['types.SystemTemplateKeys']] = None,
        required: Optional[Iterable['types.SystemTemplateKeys']] = None,
        optional: Optional[Iterable['types.SystemTemplateKeys']] = None,
        relations: Optional[Mapping['types.SystemTemplateRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SystemTemplateKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SystemTemplate_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SystemTemplate_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SystemTemplate_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SystemTemplate_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "SystemTemplate" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SystemTemplate / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SystemTemplate',
            }
        )
        _created_partial_types.add(name)


class ComponentCategory(bases.BaseComponentCategory):
    """Represents a ComponentCategory record"""

    id: _int
    name: _str
    templates: Optional[List['models.ComponentTemplate']] = None
    schemas: Optional[List['models.ComponentSchema']] = None
    data: Optional[List['models.ComponentData']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ComponentCategoryKeys']] = None,
        exclude: Optional[Iterable['types.ComponentCategoryKeys']] = None,
        required: Optional[Iterable['types.ComponentCategoryKeys']] = None,
        optional: Optional[Iterable['types.ComponentCategoryKeys']] = None,
        relations: Optional[Mapping['types.ComponentCategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ComponentCategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ComponentCategory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ComponentCategory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ComponentCategory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ComponentCategory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ComponentCategory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ComponentCategory_relational_fields:
                        raise errors.UnknownRelationalFieldError('ComponentCategory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ComponentCategory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ComponentCategory',
            }
        )
        _created_partial_types.add(name)


class ComponentTemplate(bases.BaseComponentTemplate):
    """Represents a ComponentTemplate record"""

    id: _int
    categoryId: _int
    name: _str
    content: _str
    category: Optional['models.ComponentCategory'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ComponentTemplateKeys']] = None,
        exclude: Optional[Iterable['types.ComponentTemplateKeys']] = None,
        required: Optional[Iterable['types.ComponentTemplateKeys']] = None,
        optional: Optional[Iterable['types.ComponentTemplateKeys']] = None,
        relations: Optional[Mapping['types.ComponentTemplateRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ComponentTemplateKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ComponentTemplate_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ComponentTemplate_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ComponentTemplate_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ComponentTemplate_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ComponentTemplate_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ComponentTemplate_relational_fields:
                        raise errors.UnknownRelationalFieldError('ComponentTemplate', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ComponentTemplate / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ComponentTemplate',
            }
        )
        _created_partial_types.add(name)


class ComponentSchema(bases.BaseComponentSchema):
    """Represents a ComponentSchema record"""

    id: _int
    categoryId: _int
    name: _str
    content: _str
    category: Optional['models.ComponentCategory'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ComponentSchemaKeys']] = None,
        exclude: Optional[Iterable['types.ComponentSchemaKeys']] = None,
        required: Optional[Iterable['types.ComponentSchemaKeys']] = None,
        optional: Optional[Iterable['types.ComponentSchemaKeys']] = None,
        relations: Optional[Mapping['types.ComponentSchemaRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ComponentSchemaKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ComponentSchema_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ComponentSchema_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ComponentSchema_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ComponentSchema_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ComponentSchema_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ComponentSchema_relational_fields:
                        raise errors.UnknownRelationalFieldError('ComponentSchema', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ComponentSchema / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ComponentSchema',
            }
        )
        _created_partial_types.add(name)


class ComponentData(bases.BaseComponentData):
    """Represents a ComponentData record"""

    id: _int
    categoryId: _int
    name: _str
    content: _str
    category: Optional['models.ComponentCategory'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ComponentDataKeys']] = None,
        exclude: Optional[Iterable['types.ComponentDataKeys']] = None,
        required: Optional[Iterable['types.ComponentDataKeys']] = None,
        optional: Optional[Iterable['types.ComponentDataKeys']] = None,
        relations: Optional[Mapping['types.ComponentDataRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ComponentDataKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ComponentData_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ComponentData_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ComponentData_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ComponentData_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ComponentData_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ComponentData_relational_fields:
                        raise errors.UnknownRelationalFieldError('ComponentData', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ComponentData / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ComponentData',
            }
        )
        _created_partial_types.add(name)


class QuestionComponentMapping(bases.BaseQuestionComponentMapping):
    """Represents a QuestionComponentMapping record"""

    id: _int
    content: _str

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuestionComponentMappingKeys']] = None,
        exclude: Optional[Iterable['types.QuestionComponentMappingKeys']] = None,
        required: Optional[Iterable['types.QuestionComponentMappingKeys']] = None,
        optional: Optional[Iterable['types.QuestionComponentMappingKeys']] = None,
        relations: Optional[Mapping['types.QuestionComponentMappingRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuestionComponentMappingKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _QuestionComponentMapping_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _QuestionComponentMapping_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _QuestionComponentMapping_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _QuestionComponentMapping_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "QuestionComponentMapping" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid QuestionComponentMapping / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'QuestionComponentMapping',
            }
        )
        _created_partial_types.add(name)


class Config(bases.BaseConfig):
    """Represents a Config record"""

    id: _int
    key: _str
    content: _str

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ConfigKeys']] = None,
        exclude: Optional[Iterable['types.ConfigKeys']] = None,
        required: Optional[Iterable['types.ConfigKeys']] = None,
        optional: Optional[Iterable['types.ConfigKeys']] = None,
        relations: Optional[Mapping['types.ConfigRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ConfigKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Config_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Config_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Config_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Config_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "Config" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Config / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Config',
            }
        )
        _created_partial_types.add(name)



_Category_relational_fields: Set[str] = {
        'difficultyDescriptions',
        'sections',
        'complexityGuidelines',
    }
_Category_fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficultyDescriptions', {
            'name': 'difficultyDescriptions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DifficultyDescription\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Section\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('complexityGuidelines', {
            'name': 'complexityGuidelines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ComplexityGuideline\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DifficultyLevel_relational_fields: Set[str] = {
        'difficultyDescriptions',
        'vocabulary',
    }
_DifficultyLevel_fields: Dict['types.DifficultyLevelKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('level', {
            'name': 'level',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficultyDescriptions', {
            'name': 'difficultyDescriptions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DifficultyDescription\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('vocabulary', {
            'name': 'vocabulary',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Vocabulary\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DifficultyDescription_relational_fields: Set[str] = {
        'category',
        'difficultyLevel',
    }
_DifficultyDescription_fields: Dict['types.DifficultyDescriptionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficultyLevelId', {
            'name': 'difficultyLevelId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
        ('difficultyLevel', {
            'name': 'difficultyLevel',
            'is_list': False,
            'optional': True,
            'type': 'models.DifficultyLevel',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Exam_relational_fields: Set[str] = {
        'stages',
        'vocabulary',
    }
_Exam_fields: Dict['types.ExamKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stages', {
            'name': 'stages',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Stage\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('vocabulary', {
            'name': 'vocabulary',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Vocabulary\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Stage_relational_fields: Set[str] = {
        'exam',
        'sections',
    }
_Stage_fields: Dict['types.StageKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('examId', {
            'name': 'examId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('stageNumber', {
            'name': 'stageNumber',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('exam', {
            'name': 'exam',
            'is_list': False,
            'optional': True,
            'type': 'models.Exam',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Section\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Section_relational_fields: Set[str] = {
        'stage',
        'category',
        'typeCounts',
    }
_Section_fields: Dict['types.SectionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('stageId', {
            'name': 'stageId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalQuestions', {
            'name': 'totalQuestions',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('stage', {
            'name': 'stage',
            'is_list': False,
            'optional': True,
            'type': 'models.Stage',
            'is_relational': True,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
        ('typeCounts', {
            'name': 'typeCounts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SectionQuestionTypeCount\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_QuestionType_relational_fields: Set[str] = {
        'sectionCounts',
        'promptComponents',
    }
_QuestionType_fields: Dict['types.QuestionTypeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('options', {
            'name': 'options',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nomenclature', {
            'name': 'nomenclature',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('systemInstructionTemplate', {
            'name': 'systemInstructionTemplate',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionCounts', {
            'name': 'sectionCounts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SectionQuestionTypeCount\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('promptComponents', {
            'name': 'promptComponents',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.QuestionTypePromptComponent\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SectionQuestionTypeCount_relational_fields: Set[str] = {
        'section',
        'questionType',
    }
_SectionQuestionTypeCount_fields: Dict['types.SectionQuestionTypeCountKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionId', {
            'name': 'sectionId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('questionTypeId', {
            'name': 'questionTypeId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('count', {
            'name': 'count',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('section', {
            'name': 'section',
            'is_list': False,
            'optional': True,
            'type': 'models.Section',
            'is_relational': True,
            'documentation': None,
        }),
        ('questionType', {
            'name': 'questionType',
            'is_list': False,
            'optional': True,
            'type': 'models.QuestionType',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PromptComponent_relational_fields: Set[str] = {
        'values',
    }
_PromptComponent_fields: Dict['types.PromptComponentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('values', {
            'name': 'values',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PromptComponentValue\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PromptComponentValue_relational_fields: Set[str] = {
        'component',
        'questionTypes',
    }
_PromptComponentValue_fields: Dict['types.PromptComponentValueKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('componentId', {
            'name': 'componentId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('value', {
            'name': 'value',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('component', {
            'name': 'component',
            'is_list': False,
            'optional': True,
            'type': 'models.PromptComponent',
            'is_relational': True,
            'documentation': None,
        }),
        ('questionTypes', {
            'name': 'questionTypes',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.QuestionTypePromptComponent\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_QuestionTypePromptComponent_relational_fields: Set[str] = {
        'questionType',
        'componentValue',
    }
_QuestionTypePromptComponent_fields: Dict['types.QuestionTypePromptComponentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('questionTypeId', {
            'name': 'questionTypeId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('componentValueId', {
            'name': 'componentValueId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('questionType', {
            'name': 'questionType',
            'is_list': False,
            'optional': True,
            'type': 'models.QuestionType',
            'is_relational': True,
            'documentation': None,
        }),
        ('componentValue', {
            'name': 'componentValue',
            'is_list': False,
            'optional': True,
            'type': 'models.PromptComponentValue',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Vocabulary_relational_fields: Set[str] = {
        'exam',
        'difficultyLevel',
    }
_Vocabulary_fields: Dict['types.VocabularyKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('word', {
            'name': 'word',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('definition', {
            'name': 'definition',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('examId', {
            'name': 'examId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficultyLevelId', {
            'name': 'difficultyLevelId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('exam', {
            'name': 'exam',
            'is_list': False,
            'optional': True,
            'type': 'models.Exam',
            'is_relational': True,
            'documentation': None,
        }),
        ('difficultyLevel', {
            'name': 'difficultyLevel',
            'is_list': False,
            'optional': True,
            'type': 'models.DifficultyLevel',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ComplexityGuideline_relational_fields: Set[str] = {
        'category',
    }
_ComplexityGuideline_fields: Dict['types.ComplexityGuidelineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('guideline', {
            'name': 'guideline',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SystemTemplate_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_SystemTemplate_fields: Dict['types.SystemTemplateKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ComponentCategory_relational_fields: Set[str] = {
        'templates',
        'schemas',
        'data',
    }
_ComponentCategory_fields: Dict['types.ComponentCategoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('templates', {
            'name': 'templates',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ComponentTemplate\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('schemas', {
            'name': 'schemas',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ComponentSchema\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('data', {
            'name': 'data',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ComponentData\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ComponentTemplate_relational_fields: Set[str] = {
        'category',
    }
_ComponentTemplate_fields: Dict['types.ComponentTemplateKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.ComponentCategory',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ComponentSchema_relational_fields: Set[str] = {
        'category',
    }
_ComponentSchema_fields: Dict['types.ComponentSchemaKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.ComponentCategory',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ComponentData_relational_fields: Set[str] = {
        'category',
    }
_ComponentData_fields: Dict['types.ComponentDataKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.ComponentCategory',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_QuestionComponentMapping_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_QuestionComponentMapping_fields: Dict['types.QuestionComponentMappingKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Config_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_Config_fields: Dict['types.ConfigKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('key', {
            'name': 'key',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Category)
model_rebuild(DifficultyLevel)
model_rebuild(DifficultyDescription)
model_rebuild(Exam)
model_rebuild(Stage)
model_rebuild(Section)
model_rebuild(QuestionType)
model_rebuild(SectionQuestionTypeCount)
model_rebuild(PromptComponent)
model_rebuild(PromptComponentValue)
model_rebuild(QuestionTypePromptComponent)
model_rebuild(Vocabulary)
model_rebuild(ComplexityGuideline)
model_rebuild(SystemTemplate)
model_rebuild(ComponentCategory)
model_rebuild(ComponentTemplate)
model_rebuild(ComponentSchema)
model_rebuild(ComponentData)
model_rebuild(QuestionComponentMapping)
model_rebuild(Config)
